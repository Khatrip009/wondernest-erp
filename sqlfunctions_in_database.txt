[
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.add_notification(p_user_id uuid, p_title text, p_message text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  INSERT INTO public.notifications (user_id, title, message, is_read)\r\n  VALUES (p_user_id, p_title, p_message, false);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.add_notification(p_user_id uuid, p_title text, p_message text, p_link text DEFAULT NULL::text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  INSERT INTO public.notifications (user_id, title, message, link, is_read)\r\n  VALUES (p_user_id, p_title, p_message, p_link, false);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.archive_old_records()\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nBEGIN\r\n  -- Soft‑delete graduated students older than 2 years\r\n  UPDATE public.students\r\n    SET deleted_at = NOW()\r\n    WHERE status = 'graduated' AND joining_date < CURRENT_DATE - INTERVAL '2 years';\r\n\r\n  -- Delete activity logs older than 1 year\r\n  DELETE FROM public.activity_logs WHERE created_at < CURRENT_DATE - INTERVAL '1 year';\r\n\r\n  -- Delete old notifications (older than 3 months)\r\n  DELETE FROM public.notifications WHERE created_at < CURRENT_DATE - INTERVAL '3 months';\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.assign_grade()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  total_marks integer;\r\n  pct numeric;\r\nBEGIN\r\n  SELECT e.total_marks INTO total_marks\r\n    FROM public.exams e WHERE e.id = NEW.exam_id;\r\n\r\n  IF total_marks IS NOT NULL AND total_marks > 0 THEN\r\n    pct := (NEW.marks_obtained * 100.0) / total_marks;\r\n    NEW.grade := CASE\r\n      WHEN pct >= 90 THEN 'A+'\r\n      WHEN pct >= 80 THEN 'A'\r\n      WHEN pct >= 70 THEN 'B+'\r\n      WHEN pct >= 60 THEN 'B'\r\n      WHEN pct >= 50 THEN 'C'\r\n      ELSE 'D'\r\n    END;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.audit_all_tables()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nDECLARE\r\n  v_user_id   uuid;\r\n  v_org_id    integer;\r\n  v_branch_id bigint;\r\nBEGIN\r\n  v_user_id := auth.uid();\r\n  IF v_user_id IS NULL THEN RETURN COALESCE(NEW, OLD); END IF;\r\n\r\n  SELECT organization_id, branch_id INTO v_org_id, v_branch_id\r\n    FROM public.profiles WHERE id = v_user_id;\r\n\r\n  INSERT INTO public.activity_logs (\r\n    user_id, organization_id, branch_id,\r\n    action, entity_type, entity_id, changes,\r\n    ip_address, user_agent\r\n  ) VALUES (\r\n    v_user_id, v_org_id, v_branch_id,\r\n    TG_OP, TG_TABLE_NAME, COALESCE(NEW.id, OLD.id),\r\n    CASE TG_OP\r\n      WHEN 'INSERT' THEN to_jsonb(NEW)\r\n      WHEN 'UPDATE' THEN jsonb_build_object('old', to_jsonb(OLD), 'new', to_jsonb(NEW))\r\n      WHEN 'DELETE' THEN to_jsonb(OLD)\r\n    END,\r\n    inet_client_addr(),\r\n    current_setting('request.headers', true)::json->>'user-agent'\r\n  );\r\n  RETURN COALESCE(NEW, OLD);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_create_expense_voucher()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_voucher_no TEXT;\r\n  v_vtype_id INTEGER;\r\n  v_journal_id INTEGER;\r\nBEGIN\r\n  SELECT id INTO v_journal_id FROM journal_entries WHERE reference = 'Expense #' || NEW.id;\r\n  SELECT id INTO v_vtype_id FROM voucher_types WHERE code = 'payment';\r\n  v_voucher_no := generate_voucher_no('PAY');\r\n\r\n  INSERT INTO vouchers (voucher_no, voucher_type_id, entry_date, reference, description, journal_entry_id)\r\n  VALUES (v_voucher_no, v_vtype_id, NEW.expense_date, 'Expense #' || NEW.id, NEW.description, v_journal_id);\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_create_income_voucher()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_voucher_no TEXT;\r\n  v_vtype_id INTEGER;\r\n  v_journal_id INTEGER;\r\nBEGIN\r\n  SELECT id INTO v_journal_id FROM journal_entries WHERE reference = 'Income #' || NEW.id;\r\n  SELECT id INTO v_vtype_id FROM voucher_types WHERE code = 'receipt';\r\n  v_voucher_no := generate_voucher_no('REC');\r\n\r\n  INSERT INTO vouchers (voucher_no, voucher_type_id, entry_date, reference, description, journal_entry_id)\r\n  VALUES (v_voucher_no, v_vtype_id, NEW.income_date, 'Income #' || NEW.id, NEW.description, v_journal_id);\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_create_purchase_voucher()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_voucher_no TEXT;\r\n  v_vtype_id INTEGER;\r\n  v_journal_id INTEGER;\r\nBEGIN\r\n  IF NEW.transaction_type = 'purchase' THEN\r\n    SELECT id INTO v_journal_id FROM journal_entries WHERE reference = 'Inventory Purchase #' || NEW.id;\r\n    SELECT id INTO v_vtype_id FROM voucher_types WHERE code = 'purchase';\r\n    v_voucher_no := generate_voucher_no('PUR');\r\n\r\n    INSERT INTO vouchers (voucher_no, voucher_type_id, entry_date, reference, description, journal_entry_id)\r\n    VALUES (v_voucher_no, v_vtype_id, CURRENT_DATE, 'Inventory Purchase #' || NEW.id, 'Stock purchase', v_journal_id);\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_create_receipt()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_receipt_no TEXT;\r\n  v_student_id INTEGER;\r\nBEGIN\r\n  v_receipt_no := generate_receipt_number();\r\n\r\n  -- Get student_id from student_fees\r\n  SELECT student_id INTO v_student_id\r\n  FROM student_fees\r\n  WHERE id = NEW.student_fee_id;\r\n\r\n  INSERT INTO receipts (receipt_no, student_id, payment_id, receipt_date, amount, generated_by)\r\n  VALUES (v_receipt_no, v_student_id, NEW.id, NEW.payment_date, NEW.amount, NULL);\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_create_salary_voucher()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_voucher_no TEXT;\r\n  v_vtype_id INTEGER;\r\n  v_journal_id INTEGER;\r\nBEGIN\r\n  SELECT id INTO v_journal_id FROM journal_entries WHERE reference = 'Salary #' || NEW.id;\r\n  SELECT id INTO v_vtype_id FROM voucher_types WHERE code = 'payment';\r\n\r\n  v_voucher_no := generate_voucher_no('PAY');\r\n\r\n  INSERT INTO vouchers (\r\n    voucher_no, voucher_type_id, entry_date, reference, description,\r\n    journal_entry_id, branch_id, financial_year_id\r\n  )\r\n  VALUES (\r\n    v_voucher_no, v_vtype_id, NEW.payment_date, 'Salary #' || NEW.id,\r\n    'Salary paid to teacher', v_journal_id,\r\n    NEW.branch_id, NEW.financial_year_id\r\n  );\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_create_sales_voucher()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_voucher_no TEXT;\r\n  v_vtype_id INTEGER;\r\n  v_journal_id INTEGER;\r\nBEGIN\r\n  IF NEW.transaction_type = 'issue' THEN\r\n    SELECT id INTO v_journal_id FROM journal_entries WHERE reference = 'Inventory Issue #' || NEW.id;\r\n    SELECT id INTO v_vtype_id FROM voucher_types WHERE code = 'sales';\r\n    v_voucher_no := generate_voucher_no('SAL');\r\n\r\n    INSERT INTO vouchers (voucher_no, voucher_type_id, entry_date, reference, description, journal_entry_id)\r\n    VALUES (v_voucher_no, v_vtype_id, CURRENT_DATE, 'Inventory Issue #' || NEW.id, 'Issued to student', v_journal_id);\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_issue_certificate()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_cert_no text;\r\nBEGIN\r\n  IF NEW.result = 'Pass' THEN\r\n    v_cert_no := 'CERT-' || to_char(NEW.completion_date, 'YYYYMMDD') || '-' || NEW.student_id || '-' || NEW.level_id;\r\n    INSERT INTO public.certificates (certificate_no, student_id, course_id, level_id, issue_date, branch_id, financial_year_id)\r\n    VALUES (v_cert_no, NEW.student_id, NEW.course_id, NEW.level_id, NEW.completion_date, NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_expense()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_cash_account_id    integer;\r\n  v_expense_account_id integer;\r\n  v_itc_account_id     integer;\r\n  v_journal_id         integer;\r\n  v_net_amount         numeric;\r\n  v_gst_amount         numeric;\r\nBEGIN\r\n  v_cash_account_id    := get_account_id_by_code('1001', NEW.branch_id, NEW.financial_year_id);\r\n  v_expense_account_id := get_account_id_by_code('5005', NEW.branch_id, NEW.financial_year_id);\r\n  v_itc_account_id     := get_account_id_by_code('1200', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  v_gst_amount := COALESCE(NEW.gst_amount, 0);\r\n  v_net_amount := NEW.amount - v_gst_amount;\r\n\r\n  INSERT INTO public.journal_entries (entry_date, reference, description, is_posted, branch_id, financial_year_id)\r\n  VALUES (NEW.expense_date, 'Expense #' || NEW.id, NEW.description, true, NEW.branch_id, NEW.financial_year_id)\r\n  RETURNING id INTO v_journal_id;\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_expense_account_id, v_net_amount, 0, 'Expense (net)', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  IF NEW.itc_eligible AND v_gst_amount > 0 THEN\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_itc_account_id, v_gst_amount, 0, 'ITC claimed', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_cash_account_id, 0, NEW.amount, 'Cash paid', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_fee_payment()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_cash_account_id    integer;\r\n  v_fee_income_account integer;\r\n  v_cgst_payable       integer;\r\n  v_sgst_payable       integer;\r\n  v_igst_payable       integer;\r\n  v_journal_id         integer;\r\n  v_base_amount        numeric;\r\n  v_tax_amount         numeric;\r\n  v_cgst_amount        numeric := 0;\r\n  v_sgst_amount        numeric := 0;\r\n  v_igst_amount        numeric := 0;\r\n  v_org_state          varchar(2);\r\n  v_place_of_supply    varchar(2);\r\n  v_split              jsonb;\r\n  v_tax_rate           numeric;\r\nBEGIN\r\n  v_cash_account_id    := get_account_id_by_code('1001', NEW.branch_id, NEW.financial_year_id);\r\n  v_fee_income_account := get_account_id_by_code('4001', NEW.branch_id, NEW.financial_year_id);\r\n  v_cgst_payable       := get_account_id_by_code('2504', NEW.branch_id, NEW.financial_year_id);\r\n  v_sgst_payable       := get_account_id_by_code('2503', NEW.branch_id, NEW.financial_year_id);\r\n  v_igst_payable       := get_account_id_by_code('2505', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  -- Base and tax amounts are already correct from the modal\r\n  v_base_amount := COALESCE(NEW.base_amount, NEW.amount);\r\n  v_tax_amount  := COALESCE(NEW.tax_amount, 0);\r\n\r\n  -- Compute tax rate from the payment itself (tax_amount / base_amount * 100)\r\n  IF v_tax_amount > 0 AND v_base_amount > 0 THEN\r\n    v_tax_rate := (v_tax_amount / v_base_amount) * 100;\r\n\r\n    SELECT state_code INTO v_org_state FROM public.organization\r\n    WHERE id = (SELECT organization_id FROM branches WHERE id = NEW.branch_id)\r\n    LIMIT 1;\r\n    v_place_of_supply := v_org_state;\r\n\r\n    v_split := public.split_gst(v_tax_rate, v_place_of_supply, v_org_state);\r\n    v_cgst_amount := ROUND((v_tax_amount * (v_split->>'cgst')::numeric / v_tax_rate), 2);\r\n    v_sgst_amount := ROUND((v_tax_amount * (v_split->>'sgst')::numeric / v_tax_rate), 2);\r\n    v_igst_amount := v_tax_amount - v_cgst_amount - v_sgst_amount;\r\n  END IF;\r\n\r\n  INSERT INTO public.journal_entries (\r\n    entry_date, reference, description, is_posted,\r\n    branch_id, financial_year_id\r\n  ) VALUES (\r\n    NEW.payment_date, 'Fee Payment #' || NEW.id, 'Student fee collection', true,\r\n    NEW.branch_id, NEW.financial_year_id\r\n  ) RETURNING id INTO v_journal_id;\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_cash_account_id, NEW.amount, 0, 'Cash received', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_fee_income_account, 0, v_base_amount, 'Fee income', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  IF v_cgst_amount > 0 THEN\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_cgst_payable, 0, v_cgst_amount, 'CGST payable', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n  IF v_sgst_amount > 0 THEN\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_sgst_payable, 0, v_sgst_amount, 'SGST payable', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n  IF v_igst_amount > 0 THEN\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_igst_payable, 0, v_igst_amount, 'IGST payable', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_income()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_cash_account_id   integer;\r\n  v_income_account_id integer;\r\n  v_cgst_payable      integer;\r\n  v_sgst_payable      integer;\r\n  v_igst_payable      integer;\r\n  v_journal_id        integer;\r\n  v_org_state         varchar(2);\r\n  v_split             jsonb;\r\n  v_tax_rate          numeric;\r\n  v_cgst              numeric := 0;\r\n  v_sgst              numeric := 0;\r\n  v_igst              numeric := 0;\r\nBEGIN\r\n  v_cash_account_id   := get_account_id_by_code('1001', NEW.branch_id, NEW.financial_year_id);\r\n  v_income_account_id := get_account_id_by_code('4002', NEW.branch_id, NEW.financial_year_id);\r\n  v_cgst_payable      := get_account_id_by_code('2504', NEW.branch_id, NEW.financial_year_id);\r\n  v_sgst_payable      := get_account_id_by_code('2503', NEW.branch_id, NEW.financial_year_id);\r\n  v_igst_payable      := get_account_id_by_code('2505', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  SELECT state_code INTO v_org_state FROM public.organization LIMIT 1;\r\n\r\n  IF NEW.tax_rate_id IS NOT NULL AND NEW.tax_amount > 0 THEN\r\n    SELECT rate INTO v_tax_rate FROM public.tax_rates WHERE id = NEW.tax_rate_id;\r\n    v_split := public.split_gst(v_tax_rate, v_org_state, v_org_state);\r\n    v_cgst := ROUND((NEW.tax_amount * (v_split->>'cgst')::numeric / v_tax_rate), 2);\r\n    v_sgst := ROUND((NEW.tax_amount * (v_split->>'sgst')::numeric / v_tax_rate), 2);\r\n    v_igst := NEW.tax_amount - v_cgst - v_sgst;\r\n  END IF;\r\n\r\n  INSERT INTO public.journal_entries (entry_date, reference, description, is_posted, branch_id, financial_year_id)\r\n  VALUES (NEW.income_date, 'Income #' || NEW.id, NEW.description, true, NEW.branch_id, NEW.financial_year_id)\r\n  RETURNING id INTO v_journal_id;\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_cash_account_id, NEW.amount, 0, 'Cash received', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_income_account_id, 0, NEW.base_amount, 'Income (taxable)', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  IF v_cgst > 0 THEN\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_cgst_payable, 0, v_cgst, 'CGST payable', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n  IF v_sgst > 0 THEN\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_sgst_payable, 0, v_sgst, 'SGST payable', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n  IF v_igst > 0 THEN\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_igst_payable, 0, v_igst, 'IGST payable', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_inventory_issue()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_inventory_account_id  integer;\r\n  v_expense_account_id    integer;\r\n  v_journal_id            integer;\r\n  v_amount                numeric;\r\nBEGIN\r\n  IF NEW.transaction_type = 'issue' THEN\r\n    v_inventory_account_id := get_account_id_by_code('1004', NEW.branch_id, NEW.financial_year_id);\r\n    v_expense_account_id   := get_account_id_by_code('5004', NEW.branch_id, NEW.financial_year_id);\r\n\r\n    v_amount := NEW.quantity * COALESCE(NEW.unit_price, 0);\r\n\r\n    INSERT INTO public.journal_entries (entry_date, reference, description, is_posted, branch_id, financial_year_id)\r\n    VALUES (CURRENT_DATE, 'Inventory Issue #' || NEW.id, 'Issued to student', true, NEW.branch_id, NEW.financial_year_id)\r\n    RETURNING id INTO v_journal_id;\r\n\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_expense_account_id, v_amount, 0, 'Supplies issued', NEW.branch_id, NEW.financial_year_id);\r\n\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_inventory_account_id, 0, v_amount, 'Inventory decrease', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_inventory_purchase()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_inventory_account_id  integer;\r\n  v_itc_account_id        integer;\r\n  v_payable_account_id    integer;\r\n  v_journal_id            integer;\r\n  v_amount                numeric;\r\n  v_tax_amount            numeric := 0;\r\nBEGIN\r\n  IF NEW.transaction_type = 'purchase' THEN\r\n    v_inventory_account_id := get_account_id_by_code('1004', NEW.branch_id, NEW.financial_year_id);\r\n    v_itc_account_id       := get_account_id_by_code('1200', NEW.branch_id, NEW.financial_year_id);\r\n    v_payable_account_id   := get_account_id_by_code('2001', NEW.branch_id, NEW.financial_year_id);\r\n\r\n    v_amount := NEW.quantity * COALESCE(NEW.unit_price, 0);\r\n    v_tax_amount := COALESCE(NEW.total_amount, 0) - v_amount;\r\n\r\n    INSERT INTO public.journal_entries (entry_date, reference, description, is_posted, branch_id, financial_year_id)\r\n    VALUES (CURRENT_DATE, 'Inventory Purchase #' || NEW.id, 'Stock purchase', true, NEW.branch_id, NEW.financial_year_id)\r\n    RETURNING id INTO v_journal_id;\r\n\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_inventory_account_id, v_amount, 0, 'Inventory increase', NEW.branch_id, NEW.financial_year_id);\r\n\r\n    IF v_tax_amount > 0 THEN\r\n      INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n      VALUES (v_journal_id, v_itc_account_id, v_tax_amount, 0, 'ITC claimed', NEW.branch_id, NEW.financial_year_id);\r\n    END IF;\r\n\r\n    INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n    VALUES (v_journal_id, v_payable_account_id, 0, COALESCE(NEW.total_amount, v_amount + v_tax_amount), 'Vendor payable', NEW.branch_id, NEW.financial_year_id);\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_invoice()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_receivable_account_id INTEGER;\r\n  v_fee_income_account_id INTEGER;\r\n  v_gst_cgst_account_id INTEGER;\r\n  v_gst_sgst_account_id INTEGER;\r\n  v_gst_igst_account_id INTEGER;\r\n  v_journal_id INTEGER;\r\n  v_org_state VARCHAR(2);\r\n  v_cgst NUMERIC := 0;\r\n  v_sgst NUMERIC := 0;\r\n  v_igst NUMERIC := 0;\r\nBEGIN\r\n  IF NEW.status = 'Final' AND (OLD.status IS NULL OR OLD.status != 'Final') THEN\r\n    SELECT state_code INTO v_org_state FROM organization LIMIT 1;\r\n\r\n    SELECT id INTO v_receivable_account_id FROM chart_of_accounts WHERE account_code = '1100';\r\n    SELECT id INTO v_fee_income_account_id FROM chart_of_accounts WHERE account_code = '4001';\r\n    SELECT id INTO v_gst_cgst_account_id FROM chart_of_accounts WHERE account_code = '2001';\r\n    SELECT id INTO v_gst_sgst_account_id FROM chart_of_accounts WHERE account_code = '2002';\r\n    SELECT id INTO v_gst_igst_account_id FROM chart_of_accounts WHERE account_code = '2003';\r\n\r\n    INSERT INTO journal_entries (entry_date, reference, description, is_posted)\r\n    VALUES (NEW.invoice_date, 'Invoice #' || NEW.invoice_number, 'Fee invoice generated', true)\r\n    RETURNING id INTO v_journal_id;\r\n\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES\r\n      (v_journal_id, v_receivable_account_id, NEW.grand_total, 0, 'Student receivable'),\r\n      (v_journal_id, v_fee_income_account_id, 0, NEW.total_taxable_amount, 'Fee income');\r\n\r\n    IF NEW.total_gst_amount > 0 THEN\r\n      IF NEW.place_of_supply = v_org_state OR NEW.place_of_supply IS NULL THEN\r\n        v_cgst := NEW.total_gst_amount / 2;\r\n        v_sgst := NEW.total_gst_amount / 2;\r\n        v_igst := 0;\r\n      ELSE\r\n        v_cgst := 0;\r\n        v_sgst := 0;\r\n        v_igst := NEW.total_gst_amount;\r\n      END IF;\r\n      IF v_cgst > 0 THEN\r\n        INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n        VALUES (v_journal_id, v_gst_cgst_account_id, 0, v_cgst, 'CGST payable');\r\n      END IF;\r\n      IF v_sgst > 0 THEN\r\n        INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n        VALUES (v_journal_id, v_gst_sgst_account_id, 0, v_sgst, 'SGST payable');\r\n      END IF;\r\n      IF v_igst > 0 THEN\r\n        INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n        VALUES (v_journal_id, v_gst_igst_account_id, 0, v_igst, 'IGST payable');\r\n      END IF;\r\n    END IF;\r\n\r\n    UPDATE invoices SET journal_entry_id = v_journal_id WHERE id = NEW.id;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_purchase_invoice()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_inventory_account_id INTEGER;\r\n  v_itc_account_id INTEGER;\r\n  v_vendor_payable_account_id INTEGER;\r\n  v_journal_id INTEGER;\r\n  item_record RECORD; -- ✅ declare record variable\r\nBEGIN\r\n  IF NEW.status = 'Final' AND (OLD.status IS NULL OR OLD.status != 'Final') THEN\r\n    -- Get account IDs\r\n    SELECT id INTO v_inventory_account_id FROM chart_of_accounts WHERE account_code = '1004';\r\n    SELECT id INTO v_itc_account_id FROM chart_of_accounts WHERE account_code = '1200';\r\n    SELECT id INTO v_vendor_payable_account_id FROM chart_of_accounts WHERE account_code = '2100';\r\n\r\n    IF v_inventory_account_id IS NULL OR v_vendor_payable_account_id IS NULL THEN\r\n      RAISE WARNING 'Missing chart_of_accounts for purchase invoice. Inventory: %, Vendor Payable: %', \r\n        v_inventory_account_id, v_vendor_payable_account_id;\r\n      RETURN NEW;\r\n    END IF;\r\n\r\n    -- Create journal entry\r\n    INSERT INTO journal_entries (entry_date, reference, description, is_posted)\r\n    VALUES (NEW.invoice_date, 'Purchase Invoice #' || NEW.invoice_number, 'Purchase from vendor', true)\r\n    RETURNING id INTO v_journal_id;\r\n\r\n    -- Insert lines\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES\r\n      (v_journal_id, v_inventory_account_id, NEW.total_taxable_amount, 0, 'Inventory purchase'),\r\n      (v_journal_id, v_itc_account_id, NEW.total_gst_amount, 0, 'ITC claimed'),\r\n      (v_journal_id, v_vendor_payable_account_id, 0, NEW.grand_total, 'Vendor payable');\r\n\r\n    -- Update stock\r\n    FOR item_record IN (SELECT item_id, quantity FROM purchase_invoice_items WHERE purchase_invoice_id = NEW.id)\r\n    LOOP\r\n      UPDATE inventory_items\r\n      SET current_stock = current_stock + item_record.quantity,\r\n          updated_at = NOW()\r\n      WHERE id = item_record.item_id;\r\n    END LOOP;\r\n\r\n    -- Link journal entry\r\n    UPDATE purchase_invoices SET journal_entry_id = v_journal_id WHERE id = NEW.id;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.auto_post_salary_payment()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_cash_account_id       integer;\r\n  v_salary_expense_id     integer;\r\n  v_salary_payable_id     integer;\r\n  v_journal_id            integer;\r\nBEGIN\r\n  v_cash_account_id   := get_account_id_by_code('1001', NEW.branch_id, NEW.financial_year_id);\r\n  v_salary_expense_id := get_account_id_by_code('5001', NEW.branch_id, NEW.financial_year_id);\r\n  v_salary_payable_id := get_account_id_by_code('2004', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  IF v_cash_account_id IS NULL OR v_salary_expense_id IS NULL OR v_salary_payable_id IS NULL THEN\r\n    RETURN NEW;\r\n  END IF;\r\n\r\n  INSERT INTO public.journal_entries (\r\n    entry_date, reference, description, is_posted, branch_id, financial_year_id\r\n  ) VALUES (\r\n    NEW.payment_date, 'Salary #' || NEW.id, 'Salary payment', true, NEW.branch_id, NEW.financial_year_id\r\n  ) RETURNING id INTO v_journal_id;\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_salary_expense_id, NEW.amount, 0, 'Salary expense (gross)', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  INSERT INTO public.journal_entry_lines (journal_entry_id, account_id, debit, credit, description, branch_id, financial_year_id)\r\n  VALUES (v_journal_id, v_cash_account_id, 0, NEW.amount, 'Salary paid', NEW.branch_id, NEW.financial_year_id);\r\n\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.bytea_to_text(data bytea)\n RETURNS text\n LANGUAGE c\n IMMUTABLE STRICT\nAS '$libdir/http', $function$bytea_to_text$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.can_modify_row(p_branch_id bigint, p_financial_year_id bigint)\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT\r\n    public.is_super_admin()\r\n    OR (\r\n      public.is_admin()\r\n      AND p_branch_id IN (SELECT b.id FROM branches b WHERE b.organization_id = public.get_user_org())\r\n    )\r\n  AND\r\n  (\r\n    p_financial_year_id = public.get_user_financial_year()\r\n    OR public.get_user_financial_year() IS NULL\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.can_view_row(p_branch_id bigint, p_financial_year_id bigint)\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT\r\n    -- Admin can see all rows in their org (or globally for super)\r\n    public.is_super_admin()\r\n    OR (\r\n      public.is_admin()\r\n      AND p_branch_id IN (SELECT b.id FROM branches b WHERE b.organization_id = public.get_user_org())\r\n    )\r\n    -- Normal user sees only their own branch\r\n    OR (p_branch_id = public.get_user_branch())\r\n  AND\r\n  (\r\n    p_financial_year_id = public.get_user_financial_year()\r\n    OR public.get_user_financial_year() IS NULL\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.check_stock_reorder()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_stock integer;\r\n  v_reorder integer;\r\n  v_item_name text;\r\nBEGIN\r\n  SELECT current_stock, reorder_level, item_name\r\n    INTO v_stock, v_reorder, v_item_name\r\n    FROM public.inventory_items WHERE id = NEW.item_id;\r\n\r\n  IF v_stock <= v_reorder THEN\r\n    INSERT INTO public.notifications (title, message, target_type, branch_id, financial_year_id)\r\n    VALUES (\r\n      'Low Stock Alert',\r\n      v_item_name || ' stock is at ' || v_stock || ' (reorder level: ' || v_reorder || ')',\r\n      'All', NEW.branch_id, NEW.financial_year_id\r\n    );\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.count_teacher_lectures(p_teacher_id integer, p_start date, p_end date)\n RETURNS integer\n LANGUAGE sql\n STABLE\nAS $function$\r\n  SELECT COUNT(DISTINCT s.id)\r\n  FROM attendance_sessions s\r\n  JOIN teacher_batches tb ON s.batch_id = tb.batch_id\r\n  WHERE tb.teacher_id = p_teacher_id\r\n    AND s.created_by = p_teacher_id   -- ← only sessions they created\r\n    AND s.attendance_date BETWEEN p_start AND p_end;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.create_auth_user_if_not_exists()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nBEGIN\r\n  IF NEW.user_id IS NOT NULL THEN\r\n    -- Only create the user if they don't already exist\r\n    IF NOT EXISTS (SELECT 1 FROM auth.users WHERE id = NEW.user_id::uuid) THEN\r\n      INSERT INTO auth.users (id, email, raw_user_meta_data)\r\n      VALUES (\r\n        NEW.user_id::uuid,\r\n        COALESCE(NEW.email, 'no-email-' || NEW.user_id || '@example.com'),\r\n        jsonb_build_object('role', CASE WHEN TG_TABLE_NAME = 'students' THEN 'student' ELSE 'parent' END)\r\n      );\r\n    END IF;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.create_profile_for_user()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nBEGIN\r\n  INSERT INTO public.profiles (id, email)\r\n  VALUES (NEW.id, NEW.email)\r\n  ON CONFLICT (id) DO NOTHING;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.create_receipt_on_payment()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nBEGIN\r\n  INSERT INTO public.receipts (\r\n    payment_id,\r\n    receipt_no,\r\n    receipt_date,\r\n    student_id,\r\n    amount,\r\n    branch_id,\r\n    financial_year_id\r\n  )\r\n  VALUES (\r\n    NEW.id,\r\n    'RCP-' || to_char(NEW.payment_date, 'YYYYMMDD') || '-' || lpad(NEW.id::text, 5, '0'),\r\n    NEW.payment_date,\r\n    (SELECT student_id FROM public.student_fees WHERE id = NEW.student_fee_id),\r\n    NEW.amount,\r\n    NEW.branch_id,\r\n    NEW.financial_year_id\r\n  );\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.create_voucher_for_inventory()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  new_voucher_no TEXT;\r\n  v_type_id INTEGER;\r\nBEGIN\r\n  -- Get any valid voucher_type_id\r\n  SELECT id INTO v_type_id FROM voucher_types LIMIT 1;\r\n  IF v_type_id IS NULL THEN\r\n    v_type_id := NULL;  -- or set a fallback if NULL not allowed\r\n  END IF;\r\n\r\n  new_voucher_no := generate_voucher_no();\r\n\r\n  INSERT INTO vouchers (\r\n    voucher_no,\r\n    voucher_type_id,\r\n    entry_date,\r\n    description,\r\n    reference,\r\n    branch_id,\r\n    financial_year_id,\r\n    created_at\r\n  ) VALUES (\r\n    new_voucher_no,\r\n    v_type_id,\r\n    CURRENT_DATE,\r\n    'Inventory issue: ' || COALESCE(NEW.reference, ''),\r\n    NEW.reference,\r\n    NEW.branch_id,\r\n    NEW.financial_year_id,\r\n    NOW()\r\n  );\r\n\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.current_user_role()\n RETURNS text\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT role FROM public.profiles WHERE id = auth.uid();\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.deactivate_other_sessions(p_user_id uuid)\n RETURNS void\n LANGUAGE sql\nAS $function$\r\n  UPDATE public.user_sessions\r\n  SET is_active = false\r\n  WHERE user_id = p_user_id AND is_active = true;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.finalize_credit_note(note_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  note_record RECORD;\r\n  invoice_record RECORD;\r\n  journal_id BIGINT;\r\n  ar_account_id INTEGER;\r\n  income_account_id INTEGER;\r\n  gst_cgst_account_id INTEGER;\r\n  gst_sgst_account_id INTEGER;\r\n  gst_igst_account_id INTEGER;\r\nBEGIN\r\n  SELECT * INTO note_record FROM credit_notes WHERE id = note_id AND status = 'Draft';\r\n  IF NOT FOUND THEN\r\n    RAISE EXCEPTION 'Credit note not found or already finalized.';\r\n  END IF;\r\n\r\n  SELECT * INTO invoice_record FROM invoices WHERE id = note_record.invoice_id;\r\n  IF NOT FOUND THEN\r\n    RAISE EXCEPTION 'Linked invoice not found.';\r\n  END IF;\r\n\r\n  -- Get Chart of Accounts IDs\r\n  SELECT id INTO income_account_id FROM chart_of_accounts WHERE account_code = '4001';\r\n  SELECT id INTO ar_account_id FROM chart_of_accounts WHERE account_code = '1100';\r\n  SELECT id INTO gst_cgst_account_id FROM chart_of_accounts WHERE account_code = '2001';\r\n  SELECT id INTO gst_sgst_account_id FROM chart_of_accounts WHERE account_code = '2002';\r\n  SELECT id INTO gst_igst_account_id FROM chart_of_accounts WHERE account_code = '2003';\r\n\r\n  -- Update Invoice – only reduce amounts, do NOT change status\r\n  UPDATE invoices\r\n  SET\r\n    total_taxable_amount = total_taxable_amount - note_record.taxable_amount,\r\n    total_gst_amount = total_gst_amount - note_record.total_tax_amount,\r\n    total_cgst = total_cgst - note_record.cgst,\r\n    total_sgst = total_sgst - note_record.sgst,\r\n    total_igst = total_igst - note_record.igst,\r\n    total_amount = total_amount - note_record.total_amount,\r\n    grand_total = grand_total - note_record.total_amount,\r\n    balance_due = balance_due - note_record.total_amount,\r\n    updated_at = NOW()\r\n  WHERE id = note_record.invoice_id;\r\n\r\n  -- Create Reversing Journal Entry\r\n  INSERT INTO journal_entries (entry_date, reference, description, is_posted)\r\n  VALUES (CURRENT_DATE, 'Credit Note #' || note_record.credit_note_number, 'Credit note against Invoice #' || invoice_record.invoice_number, true)\r\n  RETURNING id INTO journal_id;\r\n\r\n  INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n  VALUES\r\n    (journal_id, income_account_id, note_record.taxable_amount, 0, 'Reduction in revenue'),\r\n    (journal_id, ar_account_id, 0, note_record.total_amount, 'Reduction in receivable');\r\n\r\n  IF note_record.cgst > 0 THEN\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES (journal_id, gst_cgst_account_id, note_record.cgst, 0, 'CGST reversal');\r\n  END IF;\r\n  IF note_record.sgst > 0 THEN\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES (journal_id, gst_sgst_account_id, note_record.sgst, 0, 'SGST reversal');\r\n  END IF;\r\n  IF note_record.igst > 0 THEN\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES (journal_id, gst_igst_account_id, note_record.igst, 0, 'IGST reversal');\r\n  END IF;\r\n\r\n  UPDATE credit_notes SET status = 'Final', updated_at = NOW() WHERE id = note_id;\r\n\r\n  RETURN jsonb_build_object('success', true, 'journal_entry_id', journal_id);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.finalize_debit_note(note_id bigint)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  note_record RECORD;\r\n  invoice_record RECORD;\r\n  journal_id BIGINT;\r\n  ar_account_id INTEGER;\r\n  income_account_id INTEGER;\r\n  gst_cgst_account_id INTEGER;\r\n  gst_sgst_account_id INTEGER;\r\n  gst_igst_account_id INTEGER;\r\nBEGIN\r\n  SELECT * INTO note_record FROM debit_notes WHERE id = note_id AND status = 'Draft';\r\n  IF NOT FOUND THEN\r\n    RAISE EXCEPTION 'Debit note not found or already finalized.';\r\n  END IF;\r\n\r\n  SELECT * INTO invoice_record FROM invoices WHERE id = note_record.invoice_id;\r\n  IF NOT FOUND THEN\r\n    RAISE EXCEPTION 'Linked invoice not found.';\r\n  END IF;\r\n\r\n  SELECT id INTO income_account_id FROM chart_of_accounts WHERE account_code = '4001';\r\n  SELECT id INTO ar_account_id FROM chart_of_accounts WHERE account_code = '1100';\r\n  SELECT id INTO gst_cgst_account_id FROM chart_of_accounts WHERE account_code = '2001';\r\n  SELECT id INTO gst_sgst_account_id FROM chart_of_accounts WHERE account_code = '2002';\r\n  SELECT id INTO gst_igst_account_id FROM chart_of_accounts WHERE account_code = '2003';\r\n\r\n  -- Update Invoice – only increase amounts, do NOT change status\r\n  UPDATE invoices\r\n  SET\r\n    total_taxable_amount = total_taxable_amount + note_record.taxable_amount,\r\n    total_gst_amount = total_gst_amount + note_record.total_tax_amount,\r\n    total_cgst = total_cgst + note_record.cgst,\r\n    total_sgst = total_sgst + note_record.sgst,\r\n    total_igst = total_igst + note_record.igst,\r\n    total_amount = total_amount + note_record.total_amount,\r\n    grand_total = grand_total + note_record.total_amount,\r\n    balance_due = balance_due + note_record.total_amount,\r\n    updated_at = NOW()\r\n  WHERE id = note_record.invoice_id;\r\n\r\n  INSERT INTO journal_entries (entry_date, reference, description, is_posted)\r\n  VALUES (CURRENT_DATE, 'Debit Note #' || note_record.debit_note_number, 'Debit note against Invoice #' || invoice_record.invoice_number, true)\r\n  RETURNING id INTO journal_id;\r\n\r\n  INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n  VALUES\r\n    (journal_id, ar_account_id, note_record.total_amount, 0, 'Increase in receivable');\r\n\r\n  IF note_record.taxable_amount > 0 THEN\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES (journal_id, income_account_id, 0, note_record.taxable_amount, 'Additional revenue');\r\n  END IF;\r\n\r\n  IF note_record.cgst > 0 THEN\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES (journal_id, gst_cgst_account_id, 0, note_record.cgst, 'CGST liability');\r\n  END IF;\r\n  IF note_record.sgst > 0 THEN\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES (journal_id, gst_sgst_account_id, 0, note_record.sgst, 'SGST liability');\r\n  END IF;\r\n  IF note_record.igst > 0 THEN\r\n    INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n    VALUES (journal_id, gst_igst_account_id, 0, note_record.igst, 'IGST liability');\r\n  END IF;\r\n\r\n  UPDATE debit_notes SET status = 'Final', updated_at = NOW() WHERE id = note_id;\r\n\r\n  RETURN jsonb_build_object('success', true, 'journal_entry_id', journal_id);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.force_logout(p_session_id bigint)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nBEGIN\r\n  UPDATE public.user_sessions\r\n  SET is_active = false\r\n  WHERE id = p_session_id;\r\n\r\n  -- Optionally call Supabase admin API to revoke tokens (not directly in DB)\r\n  -- You'll need a server‑side Supabase admin client for that.\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_credit_note_number()\n RETURNS text\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  next_number INTEGER;\r\n  prefix TEXT;\r\nBEGIN\r\n  prefix := 'CN-' || TO_CHAR(CURRENT_DATE, 'YYYYMM') || '-';\r\n  SELECT COALESCE(MAX(CAST(SUBSTRING(credit_note_number FROM 'CN-[0-9]{6}-([0-9]+)') AS INTEGER)), 0) + 1\r\n  INTO next_number\r\n  FROM credit_notes;\r\n  RETURN prefix || LPAD(next_number::TEXT, 4, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_debit_note_number()\n RETURNS text\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  next_number INTEGER;\r\n  prefix TEXT;\r\nBEGIN\r\n  prefix := 'DN-' || TO_CHAR(CURRENT_DATE, 'YYYYMM') || '-';\r\n  SELECT COALESCE(MAX(CAST(SUBSTRING(debit_note_number FROM 'DN-[0-9]{6}-([0-9]+)') AS INTEGER)), 0) + 1\r\n  INTO next_number\r\n  FROM debit_notes;\r\n  RETURN prefix || LPAD(next_number::TEXT, 4, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_invoice_number()\n RETURNS text\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nDECLARE\r\n    v_year text;\r\n    v_seq int;\r\nBEGIN\r\n    v_year := to_char(current_date, 'YYYY');\r\n    \r\n    -- Lock the invoices table briefly to prevent duplicate numbers\r\n    LOCK TABLE public.invoices IN SHARE ROW EXCLUSIVE MODE;\r\n    \r\n    -- Get the next sequence number for this year (from existing invoices)\r\n    SELECT COALESCE(MAX(CAST(SUBSTRING(invoice_number FROM E'^INV-\\\\d{4}-(\\\\d+)$') AS integer)), 0) + 1\r\n    INTO v_seq\r\n    FROM public.invoices\r\n    WHERE invoice_number LIKE 'INV-' || v_year || '-%';\r\n    \r\n    -- Format: INV-YYYY-00001\r\n    RETURN 'INV-' || v_year || '-' || LPAD(v_seq::text, 5, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_monthly_salaries(p_month date DEFAULT CURRENT_DATE, p_branch_id bigint DEFAULT NULL::bigint, p_financial_year_id bigint DEFAULT NULL::bigint)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nDECLARE\r\n  teacher_record record;\r\n  total_lectures integer;\r\n  salary_amount numeric;\r\nBEGIN\r\n  FOR teacher_record IN\r\n    SELECT * FROM public.teachers\r\n    WHERE status = 'active'\r\n      AND (p_branch_id IS NULL OR branch_id = p_branch_id)\r\n  LOOP\r\n    -- Count lectures (using teacher_attendance with status 'present' or 'half_day')\r\n    SELECT COUNT(*) INTO total_lectures\r\n    FROM public.teacher_attendance\r\n    WHERE teacher_id = teacher_record.id\r\n      AND attendance_date BETWEEN date_trunc('month', p_month)::date\r\n                              AND (date_trunc('month', p_month) + INTERVAL '1 month' - INTERVAL '1 day')::date\r\n      AND status IN ('present','half_day');\r\n\r\n    IF teacher_record.salary_type = 'fixed' THEN\r\n      salary_amount := teacher_record.monthly_salary;\r\n    ELSE\r\n      salary_amount := total_lectures * COALESCE(teacher_record.per_lecture_rate, 0);\r\n    END IF;\r\n\r\n    IF salary_amount > 0 THEN\r\n      INSERT INTO public.salary_payments (\r\n        teacher_id, payment_date, amount, payment_mode,\r\n        branch_id, financial_year_id\r\n      ) VALUES (\r\n        teacher_record.id, p_month, salary_amount, 'Auto-Generated',\r\n        teacher_record.branch_id,\r\n        COALESCE(p_financial_year_id, teacher_record.financial_year_id)\r\n      );\r\n    END IF;\r\n  END LOOP;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_po_number()\n RETURNS text\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_year TEXT := EXTRACT(YEAR FROM CURRENT_DATE);\r\n  v_seq  INT;\r\nBEGIN\r\n  SELECT COALESCE(MAX(NULLIF(regexp_replace(po_number, 'PO-' || v_year || '-', ''), '')::INT), 0) + 1\r\n  INTO v_seq\r\n  FROM purchase_orders\r\n  WHERE po_number LIKE 'PO-' || v_year || '-%';\r\n  RETURN 'PO-' || v_year || '-' || LPAD(v_seq::TEXT, 4, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_receipt_number()\n RETURNS text\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  next_number INTEGER;\r\n  prefix TEXT;\r\nBEGIN\r\n  prefix := 'RCT-' || TO_CHAR(CURRENT_DATE, 'YYYYMM') || '-';\r\n  SELECT COALESCE(MAX(CAST(SUBSTRING(receipt_no FROM 'RCT-[0-9]{6}-([0-9]+)') AS INTEGER)), 0) + 1\r\n  INTO next_number\r\n  FROM receipts;\r\n  RETURN prefix || LPAD(next_number::TEXT, 4, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_voucher_no()\n RETURNS text\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  next_num BIGINT;\r\nBEGIN\r\n  next_num := nextval('voucher_no_seq');\r\n  RETURN 'INV-' || to_char(NOW(), 'YYYYMMDD') || '-' || lpad(next_num::text, 6, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.generate_voucher_no(p_prefix text)\n RETURNS text\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_seq integer;\r\n  v_year_part text;\r\nBEGIN\r\n  v_year_part := EXTRACT(YEAR FROM now())::text;\r\n\r\n  -- Lock the vouchers table to prevent concurrent reads\r\n  PERFORM 1 FROM public.vouchers WHERE voucher_no LIKE p_prefix || '-' || v_year_part || '-%' FOR UPDATE;\r\n\r\n  -- Get the next number by finding the maximum existing and adding 1\r\n  SELECT COALESCE(\r\n    MAX(NULLIF(regexp_replace(voucher_no, '^' || p_prefix || '-' || v_year_part || '-', ''), '')::integer),\r\n    0\r\n  ) + 1\r\n  INTO v_seq\r\n  FROM public.vouchers\r\n  WHERE voucher_no LIKE p_prefix || '-' || v_year_part || '-%';\r\n\r\n  RETURN p_prefix || '-' || v_year_part || '-' || LPAD(v_seq::text, 5, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_account_id_by_code(p_code text, p_branch_id bigint, p_financial_year_id bigint)\n RETURNS integer\n LANGUAGE sql\n STABLE\nAS $function$\r\n  SELECT id FROM public.chart_of_accounts\r\n  WHERE account_code = p_code\r\n    AND branch_id = p_branch_id\r\n    AND financial_year_id = p_financial_year_id\r\n  LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_auth_users_for_admin()\n RETURNS TABLE(id uuid, email text, created_at timestamp with time zone, last_sign_in_at timestamp with time zone, raw_user_meta_data jsonb, role text)\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nBEGIN\r\n  IF public.is_admin() THEN\r\n    RETURN QUERY\r\n    SELECT\r\n      au.id,\r\n      au.email::text,\r\n      au.created_at,\r\n      au.last_sign_in_at,\r\n      au.raw_user_meta_data,\r\n      COALESCE(p.role, '')::text\r\n    FROM auth.users au\r\n    LEFT JOIN public.profiles p ON p.id = au.id;   -- Fixed: changed 'users' to 'profiles'\r\n  ELSE\r\n    RAISE EXCEPTION 'Insufficient privileges: only admin or super admin can view auth users';\r\n  END IF;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_balance_sheet(as_of_date date)\n RETURNS TABLE(account_code text, account_name text, account_type text, parent_id integer, balance numeric)\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  RETURN QUERY\r\n  SELECT\r\n    a.account_code,\r\n    a.account_name,\r\n    a.account_type,\r\n    a.parent_id,\r\n    CASE\r\n      -- Asset accounts: normal debit balance (debit - credit)\r\n      WHEN a.account_type = 'asset' THEN\r\n        COALESCE(SUM(l.debit), 0) - COALESCE(SUM(l.credit), 0)\r\n      -- Expense accounts: also debit balance, but we don't show them on the balance sheet\r\n      WHEN a.account_type = 'expense' THEN\r\n        COALESCE(SUM(l.debit), 0) - COALESCE(SUM(l.credit), 0)\r\n      -- Liability / Equity / Income: normal credit balance (credit - debit)\r\n      ELSE\r\n        COALESCE(SUM(l.credit), 0) - COALESCE(SUM(l.debit), 0)\r\n    END AS balance\r\n  FROM chart_of_accounts a\r\n  LEFT JOIN journal_entry_lines l ON l.account_id = a.id\r\n  LEFT JOIN journal_entries e ON e.id = l.journal_entry_id\r\n  WHERE e.entry_date <= as_of_date\r\n  GROUP BY a.id, a.account_code, a.account_name, a.account_type, a.parent_id\r\n  ORDER BY a.account_code;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_current_branch()\n RETURNS integer\n LANGUAGE sql\n STABLE\nAS $function$\r\n  SELECT NULLIF(current_setting('request.jwt.claims', true)::json->>'branch_id', '')::int;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_dashboard_stats()\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\r\nDECLARE\r\n  org_id integer;\r\n  totalStudents integer;\r\n  totalTeachers integer;\r\n  totalBatches integer;\r\n  totalInquiries integer;\r\n  presentToday integer;\r\n  totalToday integer;\r\n  recentPayments jsonb;\r\n  upcomingExams jsonb;\r\nBEGIN\r\n  -- Get the organization of the logged-in user\r\n  SELECT organization_id INTO org_id\r\n  FROM public.profiles\r\n  WHERE id = auth.uid();\r\n\r\n  -- If no org, return zeros\r\n  IF org_id IS NULL THEN\r\n    RETURN jsonb_build_object(\r\n      'totalStudents', 0,\r\n      'activeBatches', 0,\r\n      'todayAttendance', jsonb_build_object('present', 0, 'total', 0),\r\n      'monthlyFeeCollection', 0,\r\n      'pendingFees', 0,\r\n      'totalTeachers', 0,\r\n      'activeCourses', 0,\r\n      'totalParents', 0,\r\n      'newInquiriesThisMonth', 0,\r\n      'recentInquiries', '[]'::jsonb,\r\n      'recentPayments', '[]'::jsonb,\r\n      'upcomingExams', '[]'::jsonb,\r\n      'monthlyFeeData', '[]'::jsonb,\r\n      'batchStudentData', '[]'::jsonb,\r\n      'inquiryTrendData', '[]'::jsonb,\r\n      'feeStatusData', jsonb_build_object('paid', 0, 'pending', 0),\r\n      'attendanceTrend', '[]'::jsonb,\r\n      'courseWiseStudents', '[]'::jsonb,\r\n      'lowStockItems', '[]'::jsonb,\r\n      'pendingInvoicesCount', 0,\r\n      'pendingInvoicesAmount', 0,\r\n      'todayIncome', 0\r\n    );\r\n  END IF;\r\n\r\n  -- Count students (active, not deleted) – scoped by org\r\n  SELECT COUNT(*) INTO totalStudents\r\n  FROM public.students\r\n  WHERE deleted_at IS NULL\r\n    AND status = 'active'\r\n    AND branch_id IN (SELECT id FROM public.branches WHERE organization_id = org_id);\r\n\r\n  -- Count teachers (active, not deleted)\r\n  SELECT COUNT(*) INTO totalTeachers\r\n  FROM public.teachers\r\n  WHERE deleted_at IS NULL\r\n    AND status = 'active'\r\n    AND branch_id IN (SELECT id FROM public.branches WHERE organization_id = org_id);\r\n\r\n  -- Count active batches\r\n  SELECT COUNT(*) INTO totalBatches\r\n  FROM public.batches\r\n  WHERE status = 'active'\r\n    AND branch_id IN (SELECT id FROM public.branches WHERE organization_id = org_id);\r\n\r\n  -- Count open inquiries\r\n  SELECT COUNT(*) INTO totalInquiries\r\n  FROM public.inquiries\r\n  WHERE status NOT IN ('Closed', 'Cancelled')\r\n    AND branch_id IN (SELECT id FROM public.branches WHERE organization_id = org_id);\r\n\r\n  -- Today's attendance (present & total)\r\n  SELECT\r\n    COALESCE(COUNT(*) FILTER (WHERE sa.status = 'present'), 0),\r\n    COUNT(*)\r\n  INTO presentToday, totalToday\r\n  FROM public.student_attendance sa\r\n  JOIN public.attendance_sessions s ON s.id = sa.session_id\r\n  WHERE s.attendance_date = CURRENT_DATE\r\n    AND s.branch_id IN (SELECT id FROM public.branches WHERE organization_id = org_id);\r\n\r\n  -- Recent 5 fee payments\r\n  SELECT COALESCE(jsonb_agg(p), '[]'::jsonb)\r\n  INTO recentPayments\r\n  FROM (\r\n    SELECT\r\n      fp.payment_date,\r\n      fp.amount,\r\n      fp.payment_mode,\r\n      s.first_name || ' ' || s.last_name AS student_name\r\n    FROM public.fee_payments fp\r\n    JOIN public.student_fees sf ON sf.id = fp.student_fee_id\r\n    JOIN public.students s ON s.id = sf.student_id\r\n    WHERE sf.branch_id IN (SELECT id FROM public.branches WHERE organization_id = org_id)\r\n    ORDER BY fp.payment_date DESC\r\n    LIMIT 5\r\n  ) p;\r\n\r\n  -- Upcoming 5 exams\r\n  SELECT COALESCE(jsonb_agg(e), '[]'::jsonb)\r\n  INTO upcomingExams\r\n  FROM (\r\n    SELECT\r\n      ex.exam_name,\r\n      ex.exam_date,\r\n      b.batch_name\r\n    FROM public.exams ex\r\n    LEFT JOIN public.batches b ON b.id = ex.batch_id\r\n    WHERE ex.branch_id IN (SELECT id FROM public.branches WHERE organization_id = org_id)\r\n      AND ex.exam_date >= CURRENT_DATE\r\n    ORDER BY ex.exam_date\r\n    LIMIT 5\r\n  ) e;\r\n\r\n  -- Return all dashboard data\r\n  RETURN jsonb_build_object(\r\n    'totalStudents', totalStudents,\r\n    'activeBatches', totalBatches,\r\n    'todayAttendance', jsonb_build_object('present', presentToday, 'total', totalToday),\r\n    'monthlyFeeCollection', 0,\r\n    'pendingFees', 0,\r\n    'totalTeachers', totalTeachers,\r\n    'activeCourses', 0,\r\n    'totalParents', 0,\r\n    'newInquiriesThisMonth', totalInquiries,\r\n    'recentInquiries', '[]'::jsonb,\r\n    'recentPayments', recentPayments,\r\n    'upcomingExams', upcomingExams,\r\n    'monthlyFeeData', '[]'::jsonb,\r\n    'batchStudentData', '[]'::jsonb,\r\n    'inquiryTrendData', '[]'::jsonb,\r\n    'feeStatusData', jsonb_build_object('paid', 0, 'pending', 0),\r\n    'attendanceTrend', '[]'::jsonb,\r\n    'courseWiseStudents', '[]'::jsonb,\r\n    'lowStockItems', '[]'::jsonb,\r\n    'pendingInvoicesCount', 0,\r\n    'pendingInvoicesAmount', 0,\r\n    'todayIncome', 0\r\n  );\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_dashboard_stats_by_org(p_org_id integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_branch_ids integer[];\r\n  v_result jsonb;\r\nBEGIN\r\n  v_branch_ids := ARRAY(SELECT id FROM public.branches WHERE organization_id = p_org_id);\r\n\r\n  SELECT jsonb_build_object(\r\n    'totalStudents', (SELECT COUNT(*) FROM public.students WHERE branch_id = ANY(v_branch_ids) AND deleted_at IS NULL),\r\n    'activeBatches', (SELECT COUNT(*) FROM public.batches WHERE branch_id = ANY(v_branch_ids) AND status = 'active' AND deleted_at IS NULL),\r\n    'todayAttendance', (SELECT jsonb_build_object('present', COALESCE(SUM(CASE WHEN sa.status = 'Present' THEN 1 ELSE 0 END), 0), 'total', COALESCE(COUNT(sa.id), 0)) FROM public.attendance_sessions sess LEFT JOIN public.student_attendance sa ON sa.session_id = sess.id WHERE sess.branch_id = ANY(v_branch_ids) AND sess.attendance_date = CURRENT_DATE),\r\n    'monthlyFeeCollection', (SELECT COALESCE(SUM(fp.amount), 0) FROM public.fee_payments fp JOIN public.student_fees sf ON sf.id = fp.student_fee_id WHERE sf.branch_id = ANY(v_branch_ids) AND fp.payment_date >= date_trunc('month', CURRENT_DATE)),\r\n    'pendingFees', (SELECT COALESCE(SUM(sf.final_fee - COALESCE(p.total_paid, 0)), 0) FROM public.student_fees sf LEFT JOIN (SELECT student_fee_id, SUM(amount) AS total_paid FROM public.fee_payments GROUP BY student_fee_id) p ON p.student_fee_id = sf.id WHERE sf.branch_id = ANY(v_branch_ids) AND sf.deleted_at IS NULL AND sf.status != 'Paid'),\r\n    'totalTeachers', (SELECT COUNT(*) FROM public.teachers WHERE branch_id = ANY(v_branch_ids) AND status = 'active' AND deleted_at IS NULL),\r\n    'activeCourses', (SELECT COUNT(*) FROM public.courses WHERE branch_id = ANY(v_branch_ids) AND status = true AND deleted_at IS NULL),\r\n    'totalParents', (SELECT COUNT(*) FROM public.parents WHERE branch_id = ANY(v_branch_ids) AND deleted_at IS NULL),\r\n    'newInquiriesThisMonth', (SELECT COUNT(*) FROM public.inquiries WHERE branch_id = ANY(v_branch_ids) AND created_at >= date_trunc('month', CURRENT_DATE)),\r\n    'recentInquiries', (SELECT COALESCE(jsonb_agg(t), '[]'::jsonb) FROM (SELECT inquiry_no, student_name, mobile, status FROM public.inquiries WHERE branch_id = ANY(v_branch_ids) ORDER BY created_at DESC LIMIT 5) t),\r\n    'recentPayments', (SELECT COALESCE(jsonb_agg(t), '[]'::jsonb) FROM (SELECT fp.payment_date, fp.amount, fp.payment_mode, jsonb_build_object('student', jsonb_build_object('first_name', s.first_name, 'last_name', s.last_name)) AS student_fees FROM public.fee_payments fp JOIN public.student_fees sf ON sf.id = fp.student_fee_id JOIN public.students s ON s.id = sf.student_id WHERE sf.branch_id = ANY(v_branch_ids) ORDER BY fp.payment_date DESC LIMIT 5) t),\r\n    'upcomingExams', (SELECT COALESCE(jsonb_agg(t), '[]'::jsonb) FROM (SELECT e.exam_name, e.exam_date, jsonb_build_object('batch_name', b.batch_name) AS batches FROM public.exams e JOIN public.batches b ON b.id = e.batch_id WHERE e.branch_id = ANY(v_branch_ids) AND e.exam_date >= CURRENT_DATE ORDER BY e.exam_date LIMIT 5) t),\r\n    'monthlyFeeData', (SELECT COALESCE(jsonb_agg(jsonb_build_object('month', m, 'collection', COALESCE(c, 0)) ORDER BY m), '[]'::jsonb) FROM (SELECT to_char(d, 'Mon') AS m, COALESCE(SUM(fp.amount), 0) AS c FROM generate_series(date_trunc('month', CURRENT_DATE) - INTERVAL '5 months', date_trunc('month', CURRENT_DATE), '1 month') AS d LEFT JOIN public.fee_payments fp ON date_trunc('month', fp.payment_date) = d LEFT JOIN public.student_fees sf ON sf.id = fp.student_fee_id AND sf.branch_id = ANY(v_branch_ids) GROUP BY d) sub),\r\n    'batchStudentData', (SELECT COALESCE(jsonb_agg(jsonb_build_object('name', bn, 'students', sc) ORDER BY sc DESC), '[]'::jsonb) FROM (SELECT b.batch_name AS bn, COUNT(sb.student_id) AS sc FROM public.batches b LEFT JOIN public.student_batches sb ON sb.batch_id = b.id AND sb.status = 'active' WHERE b.branch_id = ANY(v_branch_ids) AND b.status = 'active' GROUP BY b.id, b.batch_name LIMIT 10) sub),\r\n    'inquiryTrendData', (SELECT COALESCE(jsonb_agg(jsonb_build_object('month', m, 'inquiries', COALESCE(c, 0)) ORDER BY m), '[]'::jsonb) FROM (SELECT to_char(d, 'Mon') AS m, COUNT(i.id) AS c FROM generate_series(date_trunc('month', CURRENT_DATE) - INTERVAL '5 months', date_trunc('month', CURRENT_DATE), '1 month') AS d LEFT JOIN public.inquiries i ON date_trunc('month', i.created_at) = d AND i.branch_id = ANY(v_branch_ids) GROUP BY d) sub),\r\n    'feeStatusData', (SELECT jsonb_build_object('paid', COALESCE(SUM(CASE WHEN sf.status = 'Paid' THEN sf.final_fee ELSE 0 END), 0), 'pending', COALESCE(SUM(CASE WHEN sf.status != 'Paid' THEN sf.final_fee - COALESCE(p.total_paid, 0) ELSE 0 END), 0)) FROM public.student_fees sf LEFT JOIN (SELECT student_fee_id, SUM(amount) AS total_paid FROM public.fee_payments GROUP BY student_fee_id) p ON p.student_fee_id = sf.id WHERE sf.branch_id = ANY(v_branch_ids) AND sf.deleted_at IS NULL),\r\n    'attendanceTrend', (SELECT COALESCE(jsonb_agg(jsonb_build_object('day', TO_CHAR(d, 'DD'), 'percentage', COALESCE(p, 0)) ORDER BY d), '[]'::jsonb) FROM (SELECT sess.attendance_date AS d, ROUND(COUNT(CASE WHEN sa.status = 'Present' THEN 1 END)::numeric / NULLIF(COUNT(sa.id), 0) * 100, 1) AS p FROM public.attendance_sessions sess LEFT JOIN public.student_attendance sa ON sa.session_id = sess.id WHERE sess.branch_id = ANY(v_branch_ids) AND sess.attendance_date >= CURRENT_DATE - INTERVAL '6 days' GROUP BY sess.attendance_date) sub),\r\n    'courseWiseStudents', (SELECT COALESCE(jsonb_agg(jsonb_build_object('name', cn, 'students', sc) ORDER BY sc DESC), '[]'::jsonb) FROM (SELECT c.course_name AS cn, COUNT(sb.student_id) AS sc FROM public.courses c JOIN public.batches b ON b.course_id = c.id AND b.status = 'active' JOIN public.student_batches sb ON sb.batch_id = b.id AND sb.status = 'active' WHERE c.branch_id = ANY(v_branch_ids) GROUP BY c.id, c.course_name LIMIT 10) sub),\r\n    'lowStockItems', (SELECT COALESCE(jsonb_agg(row_to_json(t)), '[]'::jsonb) FROM (SELECT id, item_name, current_stock, reorder_level FROM public.inventory_items WHERE branch_id = ANY(v_branch_ids) AND current_stock <= reorder_level LIMIT 5) t),\r\n    'pendingInvoicesCount', (SELECT COUNT(*) FROM public.invoices WHERE branch_id = ANY(v_branch_ids) AND status != 'Paid' AND deleted_at IS NULL),\r\n    'pendingInvoicesAmount', (SELECT COALESCE(SUM(grand_total), 0) FROM public.invoices WHERE branch_id = ANY(v_branch_ids) AND status != 'Paid' AND deleted_at IS NULL),\r\n    'todayIncome', (SELECT COALESCE(SUM(amount), 0) FROM public.income WHERE branch_id = ANY(v_branch_ids) AND income_date = CURRENT_DATE)\r\n  ) INTO v_result;\r\n\r\n  RETURN v_result;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_dashboard_stats_test(p_org_id integer)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nBEGIN\r\n  RETURN jsonb_build_object(\r\n    'totalStudents', (SELECT COUNT(*) FROM public.students\r\n                      WHERE branch_id IN (SELECT id FROM public.branches WHERE organization_id = p_org_id)\r\n                        AND deleted_at IS NULL)\r\n  );\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_dashboard_stats_v2()\n RETURNS jsonb\n LANGUAGE plpgsql\n STABLE\nAS $function$\r\nDECLARE\r\n  total_students BIGINT;\r\n  active_batches BIGINT;\r\n  today_present BIGINT;\r\n  today_total BIGINT;\r\n  monthly_fee_collection NUMERIC;\r\n  pending_fees NUMERIC;\r\n  total_teachers BIGINT;\r\n  active_courses BIGINT;\r\n  total_parents BIGINT;\r\n  new_inquiries_month BIGINT;\r\n  recent_inquiries JSONB;\r\n  recent_payments JSONB;\r\n  upcoming_exams JSONB;\r\n  monthly_fee_data JSONB;\r\n  batch_student_data JSONB;\r\n  fee_status_paid NUMERIC;\r\n  fee_status_pending NUMERIC;\r\n  attendance_trend JSONB;\r\n  course_wise_students JSONB;\r\n  low_stock_items JSONB;\r\n  pending_invoices_count BIGINT;\r\n  pending_invoices_amount NUMERIC;\r\n  today_income NUMERIC;\r\nBEGIN\r\n  -- Simple SELECTs – no GROUP BY issues\r\n  SELECT COUNT(*) INTO total_students FROM students WHERE deleted_at IS NULL;\r\n  SELECT COUNT(*) INTO active_batches FROM batches WHERE status = 'active';\r\n  SELECT COALESCE(COUNT(*) FILTER (WHERE sa.status = 'Present'), 0), COUNT(*) INTO today_present, today_total\r\n    FROM student_attendance sa\r\n    JOIN attendance_sessions s ON sa.session_id = s.id\r\n    WHERE s.attendance_date = CURRENT_DATE;\r\n  SELECT COALESCE(SUM(amount), 0) INTO monthly_fee_collection FROM fee_payments WHERE payment_date >= DATE_TRUNC('month', CURRENT_DATE);\r\n  SELECT COALESCE(SUM(sf.final_fee - COALESCE(fp.total_paid, 0)), 0) INTO pending_fees\r\n    FROM student_fees sf\r\n    LEFT JOIN (\r\n      SELECT student_fee_id, SUM(amount) AS total_paid\r\n      FROM fee_payments\r\n      GROUP BY student_fee_id\r\n    ) fp ON sf.id = fp.student_fee_id\r\n    WHERE sf.deleted_at IS NULL AND sf.status != 'Paid';\r\n  SELECT COUNT(*) INTO total_teachers FROM teachers WHERE deleted_at IS NULL;\r\n  SELECT COUNT(*) INTO active_courses FROM courses WHERE status = true;\r\n  SELECT COUNT(*) INTO total_parents FROM parents;\r\n  SELECT COUNT(*) INTO new_inquiries_month FROM inquiries WHERE created_at >= DATE_TRUNC('month', CURRENT_DATE);\r\n\r\n  -- JSON aggregates – using simple subqueries with no alias issues\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'inquiry_no', inquiry_no,\r\n            'student_name', student_name,\r\n            'mobile', mobile,\r\n            'status', status\r\n          )\r\n        )\r\n        FROM inquiries\r\n        ORDER BY created_at DESC\r\n        LIMIT 5\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO recent_inquiries;\r\n\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'payment_date', fp.payment_date,\r\n            'amount', fp.amount,\r\n            'payment_mode', fp.payment_mode,\r\n            'student_fees', jsonb_build_object(\r\n              'student', jsonb_build_object(\r\n                'first_name', s.first_name,\r\n                'last_name', s.last_name\r\n              )\r\n            )\r\n          )\r\n        )\r\n        FROM fee_payments fp\r\n        LEFT JOIN student_fees sf ON fp.student_fee_id = sf.id\r\n        LEFT JOIN students s ON sf.student_id = s.id\r\n        ORDER BY fp.payment_date DESC\r\n        LIMIT 5\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO recent_payments;\r\n\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'exam_name', e.exam_name,\r\n            'exam_date', e.exam_date,\r\n            'batches', jsonb_build_object('batch_name', b.batch_name)\r\n          )\r\n        )\r\n        FROM exams e\r\n        LEFT JOIN batches b ON e.batch_id = b.id\r\n        WHERE e.exam_date >= CURRENT_DATE\r\n        ORDER BY e.exam_date\r\n        LIMIT 5\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO upcoming_exams;\r\n\r\n  -- monthly_fee_data\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'month', TO_CHAR(month, 'Mon'),\r\n            'collection', COALESCE(SUM(fp.amount), 0)\r\n          )\r\n        )\r\n        FROM generate_series(\r\n          DATE_TRUNC('month', CURRENT_DATE) - INTERVAL '5 months',\r\n          DATE_TRUNC('month', CURRENT_DATE),\r\n          '1 month'\r\n        ) AS month\r\n        LEFT JOIN fee_payments fp ON fp.payment_date >= month AND fp.payment_date < month + INTERVAL '1 month'\r\n        GROUP BY month\r\n        ORDER BY month\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO monthly_fee_data;\r\n\r\n  -- batch_student_data\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'name', b.batch_name,\r\n            'students', COUNT(sb.student_id)\r\n          )\r\n        )\r\n        FROM batches b\r\n        LEFT JOIN student_batches sb ON b.id = sb.batch_id AND sb.status = 'active'\r\n        WHERE b.status = 'active'\r\n        GROUP BY b.id, b.batch_name\r\n        ORDER BY b.batch_name\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO batch_student_data;\r\n\r\n  -- fee_status_data\r\n  SELECT\r\n    COALESCE(SUM(CASE WHEN sf.status = 'Paid' THEN sf.final_fee ELSE 0 END), 0),\r\n    COALESCE(SUM(CASE WHEN sf.status != 'Paid' THEN sf.final_fee - COALESCE(fp.total_paid, 0) ELSE 0 END), 0)\r\n    INTO fee_status_paid, fee_status_pending\r\n    FROM student_fees sf\r\n    LEFT JOIN (\r\n      SELECT student_fee_id, SUM(amount) AS total_paid\r\n      FROM fee_payments\r\n      GROUP BY student_fee_id\r\n    ) fp ON sf.id = fp.student_fee_id\r\n    WHERE sf.deleted_at IS NULL;\r\n\r\n  -- attendance_trend\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'day', TO_CHAR(day, 'DD'),\r\n            'percentage', ROUND(COALESCE(present_count * 100.0 / NULLIF(total_count, 0), 0), 1)\r\n          )\r\n        )\r\n        FROM generate_series(\r\n          CURRENT_DATE - INTERVAL '6 days',\r\n          CURRENT_DATE,\r\n          '1 day'\r\n        ) AS day\r\n        LEFT JOIN (\r\n          SELECT s.attendance_date,\r\n            COUNT(*) FILTER (WHERE sa.status = 'Present') AS present_count,\r\n            COUNT(*) AS total_count\r\n          FROM attendance_sessions s\r\n          JOIN student_attendance sa ON s.id = sa.session_id\r\n          WHERE s.attendance_date >= CURRENT_DATE - INTERVAL '6 days'\r\n          GROUP BY s.attendance_date\r\n        ) att ON day = att.attendance_date\r\n        GROUP BY day\r\n        ORDER BY day\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO attendance_trend;\r\n\r\n  -- course_wise_students\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'name', c.course_name,\r\n            'students', COUNT(sb.student_id)\r\n          )\r\n        )\r\n        FROM courses c\r\n        LEFT JOIN batches b ON c.id = b.course_id\r\n        LEFT JOIN student_batches sb ON b.id = sb.batch_id AND sb.status = 'active'\r\n        WHERE c.status = true\r\n        GROUP BY c.id, c.course_name\r\n        ORDER BY c.course_name\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO course_wise_students;\r\n\r\n  -- low_stock_items\r\n  SELECT COALESCE(\r\n      (SELECT jsonb_agg(\r\n          jsonb_build_object(\r\n            'item_name', item_name,\r\n            'current_stock', current_stock,\r\n            'reorder_level', reorder_level,\r\n            'id', id\r\n          )\r\n        )\r\n        FROM inventory_items\r\n        WHERE current_stock <= reorder_level AND reorder_level > 0\r\n        LIMIT 5\r\n      ),\r\n      '[]'::jsonb\r\n    ) INTO low_stock_items;\r\n\r\n  -- pending_invoices\r\n  SELECT\r\n    COUNT(*),\r\n    COALESCE(SUM(grand_total - COALESCE(paid_amount, 0)), 0)\r\n    INTO pending_invoices_count, pending_invoices_amount\r\n    FROM invoices\r\n    WHERE status = 'Final' AND status != 'Paid';\r\n\r\n  -- today_income\r\n  SELECT COALESCE(SUM(amount), 0) INTO today_income FROM fee_payments WHERE payment_date = CURRENT_DATE;\r\n\r\n  -- Build final JSON\r\n  RETURN jsonb_build_object(\r\n    'totalStudents', total_students,\r\n    'activeBatches', active_batches,\r\n    'todayAttendance', jsonb_build_object(\r\n      'present', today_present,\r\n      'total', today_total\r\n    ),\r\n    'monthlyFeeCollection', monthly_fee_collection,\r\n    'pendingFees', pending_fees,\r\n    'totalTeachers', total_teachers,\r\n    'activeCourses', active_courses,\r\n    'totalParents', total_parents,\r\n    'newInquiriesThisMonth', new_inquiries_month,\r\n    'recentInquiries', recent_inquiries,\r\n    'recentPayments', recent_payments,\r\n    'upcomingExams', upcoming_exams,\r\n    'monthlyFeeData', monthly_fee_data,\r\n    'batchStudentData', batch_student_data,\r\n    'inquiryTrendData', '[]'::jsonb,\r\n    'feeStatusData', jsonb_build_object(\r\n      'paid', fee_status_paid,\r\n      'pending', fee_status_pending\r\n    ),\r\n    'attendanceTrend', attendance_trend,\r\n    'courseWiseStudents', course_wise_students,\r\n    'lowStockItems', low_stock_items,\r\n    'pendingInvoicesCount', pending_invoices_count,\r\n    'pendingInvoicesAmount', pending_invoices_amount,\r\n    'todayIncome', today_income\r\n  );\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_inquiry_payload(new_row inquiries)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  org_id integer;\r\n  course_name text;\r\n  academy_name text;\r\nBEGIN\r\n  org_id := (SELECT organization_id FROM branches WHERE id = new_row.branch_id);\r\n  SELECT company_name INTO academy_name FROM organization WHERE id = org_id;\r\n  SELECT course_name INTO course_name FROM courses WHERE id = new_row.interested_course_id;\r\n\r\n  RETURN jsonb_build_object(\r\n    'to', new_row.email,\r\n    'organizationId', org_id,\r\n    'slug', 'inquiry_confirmation',\r\n    'context', jsonb_build_object(\r\n      'academyName', academy_name,\r\n      'parent_name', new_row.parent_name,\r\n      'student_name', new_row.student_name,\r\n      'inquiry_no', new_row.inquiry_no,\r\n      'mobile', new_row.mobile,\r\n      'course_name', course_name\r\n    ),\r\n    'branchId', new_row.branch_id\r\n  );\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_my_parent_id()\n RETURNS integer\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n    SELECT id FROM public.parents WHERE user_id = auth.uid() LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_my_student_id()\n RETURNS integer\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO ''\nAS $function$\r\n  SELECT id FROM public.students WHERE user_id = auth.uid() LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_my_student_record()\n RETURNS SETOF students\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO ''\nAS $function$\r\n  SELECT * FROM public.students\r\n  WHERE user_id = auth.uid()\r\n  LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_my_teacher_id()\n RETURNS integer\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO ''\nAS $function$\r\n  SELECT id FROM public.teachers WHERE user_id = auth.uid() LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_profit_loss()\n RETURNS TABLE(account_name text, balance numeric)\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  RETURN QUERY\r\n  SELECT\r\n    a.account_name,\r\n    SUM(COALESCE(l.credit, 0)) - SUM(COALESCE(l.debit, 0)) AS balance\r\n  FROM chart_of_accounts a\r\n  LEFT JOIN journal_entry_lines l ON l.account_id = a.id\r\n  WHERE a.account_type IN ('income', 'expense')\r\n  GROUP BY a.id\r\n  ORDER BY a.account_code;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_profit_loss(start_date date, end_date date)\n RETURNS TABLE(account_code text, account_name text, account_type text, parent_id integer, balance numeric)\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  RETURN QUERY\r\n  SELECT\r\n    a.account_code,\r\n    a.account_name,\r\n    a.account_type,\r\n    a.parent_id,\r\n    CASE\r\n      WHEN a.account_type = 'income' THEN\r\n        COALESCE(SUM(l.credit), 0) - COALESCE(SUM(l.debit), 0)\r\n      WHEN a.account_type = 'expense' THEN\r\n        COALESCE(SUM(l.debit), 0) - COALESCE(SUM(l.credit), 0)\r\n      ELSE 0\r\n    END AS balance\r\n  FROM chart_of_accounts a\r\n  LEFT JOIN journal_entry_lines l ON l.account_id = a.id\r\n  LEFT JOIN journal_entries e ON e.id = l.journal_entry_id\r\n  WHERE e.entry_date BETWEEN start_date AND end_date\r\n    AND a.account_type IN ('income', 'expense')\r\n  GROUP BY a.id, a.account_code, a.account_name, a.account_type, a.parent_id\r\n  ORDER BY a.account_code;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_teacher_id()\n RETURNS integer\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT id FROM teachers WHERE user_id = auth.uid();\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_trial_balance(as_of_date date)\n RETURNS TABLE(account_code text, account_name text, debit_total numeric, credit_total numeric)\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  RETURN QUERY\r\n  SELECT\r\n    a.account_code,\r\n    a.account_name,\r\n    COALESCE(SUM(l.debit), 0) AS debit_total,\r\n    COALESCE(SUM(l.credit), 0) AS credit_total\r\n  FROM chart_of_accounts a\r\n  LEFT JOIN journal_entry_lines l ON l.account_id = a.id\r\n  LEFT JOIN journal_entries e ON e.id = l.journal_entry_id\r\n  WHERE e.entry_date <= as_of_date\r\n  GROUP BY a.id\r\n  ORDER BY a.account_code;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_trial_balance(as_of_date date DEFAULT CURRENT_DATE, p_branch_id bigint DEFAULT NULL::bigint, p_financial_year_id bigint DEFAULT NULL::bigint)\n RETURNS TABLE(account_code text, account_name text, total_debit numeric, total_credit numeric)\n LANGUAGE plpgsql\n STABLE SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nBEGIN\r\n  RETURN QUERY\r\n  SELECT\r\n    coa.account_code,\r\n    coa.account_name,\r\n    COALESCE(SUM(jel.debit), 0) AS total_debit,\r\n    COALESCE(SUM(jel.credit), 0) AS total_credit\r\n  FROM public.chart_of_accounts coa\r\n  LEFT JOIN public.journal_entry_lines jel \r\n    ON coa.id = jel.account_id\r\n    AND (p_branch_id IS NULL OR jel.branch_id = p_branch_id)              -- ✅ scope lines\r\n    AND (p_financial_year_id IS NULL OR jel.financial_year_id = p_financial_year_id)\r\n  LEFT JOIN public.journal_entries je \r\n    ON je.id = jel.journal_entry_id\r\n    AND (je.entry_date <= as_of_date OR je.entry_date IS NULL)\r\n  WHERE (p_branch_id IS NULL OR coa.branch_id = p_branch_id)             -- accounts scoped\r\n    AND (p_financial_year_id IS NULL OR coa.financial_year_id = p_financial_year_id)\r\n  GROUP BY coa.id, coa.account_code, coa.account_name\r\n  ORDER BY coa.account_code;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_user_branch()\n RETURNS bigint\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT p.branch_id\r\n  FROM profiles p\r\n  WHERE p.id = auth.uid()\r\n  LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_user_email(user_id uuid)\n RETURNS text\n LANGUAGE sql\n SECURITY DEFINER\n SET search_path TO ''\nAS $function$\r\n  SELECT email FROM auth.users WHERE id = user_id;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_user_financial_year()\n RETURNS bigint\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT p.selected_financial_year_id\r\n  FROM profiles p\r\n  WHERE p.id = auth.uid()\r\n  LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_user_org()\n RETURNS integer\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT (p.organization_id)::integer\r\n  FROM profiles p\r\n  WHERE p.id = auth.uid()\r\n  LIMIT 1;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_user_role()\n RETURNS text\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\n  SELECT role FROM public.profiles WHERE id = auth.uid();\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.get_valid_user_ids(user_ids uuid[])\n RETURNS uuid[]\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nBEGIN\r\n  RETURN ARRAY(\r\n    SELECT id FROM auth.users WHERE id = ANY(user_ids)\r\n  );\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.has_admin_role()\n RETURNS boolean\n LANGUAGE sql\n SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid() AND role IN ('admin', 'super_admin', 'organization_admin', 'branch_admin')\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http(request http_request)\n RETURNS http_response\n LANGUAGE c\nAS '$libdir/http', $function$http_request$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_delete(uri character varying)\n RETURNS http_response\n LANGUAGE sql\nAS $function$ SELECT public.http(('DELETE', $1, NULL, NULL, NULL)::public.http_request) $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_delete(uri character varying, content character varying, content_type character varying)\n RETURNS http_response\n LANGUAGE sql\nAS $function$ SELECT public.http(('DELETE', $1, NULL, $3, $2)::public.http_request) $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_get(uri character varying)\n RETURNS http_response\n LANGUAGE sql\nAS $function$ SELECT public.http(('GET', $1, NULL, NULL, NULL)::public.http_request) $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_get(uri character varying, data jsonb)\n RETURNS http_response\n LANGUAGE sql\nAS $function$\n        SELECT public.http(('GET', $1 || '?' || public.urlencode($2), NULL, NULL, NULL)::public.http_request)\n    $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_head(uri character varying)\n RETURNS http_response\n LANGUAGE sql\nAS $function$ SELECT public.http(('HEAD', $1, NULL, NULL, NULL)::public.http_request) $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_header(field character varying, value character varying)\n RETURNS http_header\n LANGUAGE sql\nAS $function$ SELECT $1, $2 $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_list_curlopt()\n RETURNS TABLE(curlopt text, value text)\n LANGUAGE c\nAS '$libdir/http', $function$http_list_curlopt$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_patch(uri character varying, content character varying, content_type character varying)\n RETURNS http_response\n LANGUAGE sql\nAS $function$ SELECT public.http(('PATCH', $1, NULL, $3, $2)::public.http_request) $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_post(uri character varying, data jsonb)\n RETURNS http_response\n LANGUAGE sql\nAS $function$\n        SELECT public.http(('POST', $1, NULL, 'application/x-www-form-urlencoded', public.urlencode($2))::public.http_request)\n    $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_post(uri character varying, content character varying, content_type character varying)\n RETURNS http_response\n LANGUAGE sql\nAS $function$ SELECT public.http(('POST', $1, NULL, $3, $2)::public.http_request) $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_put(uri character varying, content character varying, content_type character varying)\n RETURNS http_response\n LANGUAGE sql\nAS $function$ SELECT public.http(('PUT', $1, NULL, $3, $2)::public.http_request) $function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_reset_curlopt()\n RETURNS boolean\n LANGUAGE c\nAS '$libdir/http', $function$http_reset_curlopt$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.http_set_curlopt(curlopt character varying, value character varying)\n RETURNS boolean\n LANGUAGE c\nAS '$libdir/http', $function$http_set_curlopt$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_admin()\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid()\r\n      AND role IN ('super_admin', 'organization_admin', 'admin', 'branch_admin')\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_admin_or_teacher()\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid()\r\n    AND role IN ('Super Admin', 'Admin', 'Teacher')\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_admin_user()\n RETURNS boolean\n LANGUAGE sql\n SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid()\r\n      AND role IN ('admin', 'super_admin', 'organization_admin', 'branch_admin')\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_branch_admin()\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid() AND role = 'branch_admin'\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_org_admin()\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid() AND role IN ('organization_admin', 'admin')\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_super_admin()\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid() AND role = 'super_admin'\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_teacher()\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM profiles\r\n    WHERE id = auth.uid() AND role = 'Teacher'\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.is_teacher_owner(teacher_id integer)\n RETURNS boolean\n LANGUAGE sql\n SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (\r\n    SELECT 1 FROM teachers\r\n    WHERE teachers.id = $1\r\n      AND teachers.user_id = auth.uid()\r\n  );\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.link_teacher_to_auth()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  NEW.user_id = (SELECT id FROM auth.users WHERE email = NEW.email);\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.log_activity(p_action text, p_entity_type text DEFAULT NULL::text, p_entity_id bigint DEFAULT NULL::bigint, p_details jsonb DEFAULT NULL::jsonb)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_org_id integer;\r\n  v_branch_id bigint;\r\nBEGIN\r\n  SELECT organization_id, branch_id INTO v_org_id, v_branch_id\r\n  FROM public.profiles WHERE id = auth.uid();\r\n\r\n  INSERT INTO public.activity_logs (\r\n    user_id, organization_id, branch_id,\r\n    action, entity_type, entity_id, details,\r\n    ip_address, user_agent\r\n  ) VALUES (\r\n    auth.uid(), v_org_id, v_branch_id,\r\n    p_action, p_entity_type, p_entity_id, p_details,\r\n    inet_client_addr(),\r\n    (SELECT current_setting('request.headers', true)::json->>'user-agent')\r\n  );\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.log_login(p_user_id uuid, p_session_token text DEFAULT NULL::text, p_ip_address inet DEFAULT NULL::inet, p_user_agent text DEFAULT NULL::text)\n RETURNS bigint\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_session_id bigint;\r\n  v_ip inet;\r\n  v_agent text;\r\n  v_org_id integer;\r\n  v_branch_id bigint;\r\nBEGIN\r\n  v_ip := COALESCE(p_ip_address, inet_client_addr());\r\n  v_agent := COALESCE(p_user_agent, current_setting('request.headers', true)::json->>'user-agent');\r\n\r\n  -- Create session record\r\n  INSERT INTO public.user_sessions (user_id, session_token, ip_address, user_agent)\r\n  VALUES (p_user_id, p_session_token, v_ip, v_agent)\r\n  RETURNING id INTO v_session_id;\r\n\r\n  -- Get profile info\r\n  SELECT p.organization_id, p.branch_id\r\n    INTO v_org_id, v_branch_id\r\n    FROM public.profiles p\r\n    WHERE p.id = p_user_id;\r\n\r\n  -- Log activity only if organisation exists\r\n  IF v_org_id IS NOT NULL THEN\r\n    INSERT INTO public.activity_logs (\r\n      user_id, organization_id, branch_id,\r\n      action, entity_type, entity_id, details,\r\n      ip_address, user_agent\r\n    ) VALUES (\r\n      p_user_id, v_org_id, v_branch_id,\r\n      'login', 'session', v_session_id,          -- entity is the session, id is bigint\r\n      jsonb_build_object('session_id', v_session_id, 'user_id', p_user_id),\r\n      v_ip, v_agent\r\n    );\r\n  END IF;\r\n\r\n  RETURN v_session_id;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.log_logout(p_session_id bigint, p_user_id uuid DEFAULT auth.uid())\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_org_id integer;\r\n  v_branch_id bigint;\r\nBEGIN\r\n  -- Deactivate session\r\n  UPDATE public.user_sessions\r\n  SET is_active = false,\r\n      last_active_at = now()\r\n  WHERE id = p_session_id AND user_id = p_user_id;\r\n\r\n  -- Get profile info\r\n  SELECT p.organization_id, p.branch_id\r\n    INTO v_org_id, v_branch_id\r\n    FROM public.profiles p\r\n    WHERE p.id = p_user_id;\r\n\r\n  -- Log activity\r\n  IF v_org_id IS NOT NULL THEN\r\n    INSERT INTO public.activity_logs (\r\n      user_id, organization_id, branch_id,\r\n      action, entity_type, entity_id, details,\r\n      ip_address, user_agent\r\n    ) VALUES (\r\n      p_user_id, v_org_id, v_branch_id,\r\n      'logout', 'session', p_session_id,\r\n      jsonb_build_object('session_id', p_session_id, 'user_id', p_user_id),\r\n      inet_client_addr(),\r\n      current_setting('request.headers', true)::json->>'user-agent'\r\n    );\r\n  END IF;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.log_purchase_tax()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n    INSERT INTO tax_collections (\r\n        collection_date,\r\n        amount,\r\n        category,\r\n        tax_rate_id,\r\n        branch_id,\r\n        financial_year_id\r\n    )\r\n    SELECT \r\n        NEW.invoice_date,\r\n        COALESCE(SUM(pi.cgst_amount + pi.sgst_amount + pi.igst_amount), 0),\r\n        'purchase_invoice',\r\n        NULL,\r\n        NEW.branch_id,\r\n        NEW.financial_year_id\r\n    FROM purchase_invoice_items pi\r\n    WHERE pi.purchase_invoice_id = NEW.id;\r\n    \r\n    RETURN NULL;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.log_tax_on_payment()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n    v_tax_rate numeric;\r\n    v_tax_amount numeric;\r\n    v_tax_rate_id bigint;\r\nBEGIN\r\n    SELECT cf.tax_rate_id, tr.rate\r\n    INTO v_tax_rate_id, v_tax_rate\r\n    FROM student_fees sf\r\n    JOIN course_fees cf ON sf.course_fee_id = cf.id\r\n    LEFT JOIN tax_rates tr ON cf.tax_rate_id = tr.id\r\n    WHERE sf.id = NEW.student_fee_id;\r\n\r\n    IF v_tax_rate IS NOT NULL AND v_tax_rate > 0 THEN\r\n        -- Adjust formula based on your tax‑inclusive logic\r\n        v_tax_amount := NEW.amount * (v_tax_rate / 100);\r\n\r\n        INSERT INTO tax_collections (\r\n            collection_date,\r\n            amount,\r\n            category,\r\n            payment_id,\r\n            tax_rate_id,\r\n            branch_id,\r\n            financial_year_id,\r\n            student_fee_id\r\n        ) VALUES (\r\n            NEW.payment_date,\r\n            v_tax_amount,\r\n            'fee_payment',\r\n            NEW.id,\r\n            v_tax_rate_id,\r\n            NEW.branch_id,\r\n            NEW.financial_year_id,\r\n            NEW.student_fee_id\r\n        );\r\n    END IF;\r\n\r\n    RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.next_invoice_number()\n RETURNS text\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nDECLARE\r\n    current_year INTEGER := EXTRACT(YEAR FROM CURRENT_DATE);\r\n    next_counter INTEGER;\r\n    next_number TEXT;\r\nBEGIN\r\n    -- Atomically increment the counter for the current year\r\n    INSERT INTO invoice_counters (year, counter)\r\n    VALUES (current_year, 1)\r\n    ON CONFLICT (year) DO UPDATE\r\n    SET counter = invoice_counters.counter + 1\r\n    RETURNING counter INTO next_counter;\r\n\r\n    -- Format: INV-YYYY-XXXX (zero-padded to 4 digits)\r\n    next_number := 'INV-' || current_year || '-' || LPAD(next_counter::TEXT, 4, '0');\r\n    RETURN next_number;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.next_invoice_number_v2()\n RETURNS text\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nDECLARE\r\n  next_val bigint;\r\nBEGIN\r\n  -- Lock to prevent concurrent access\r\n  PERFORM pg_advisory_xact_lock(123456);\r\n  next_val := nextval('public.invoice_seq_v2');\r\n  -- Format: INV-YYYYMM-XXXXXX (e.g., INV-202607-100000)\r\n  RETURN 'INV-' || to_char(current_date, 'YYYYMM') || '-' || LPAD(next_val::text, 6, '0');\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_exam_results()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_exam_name text;\r\nBEGIN\r\n  SELECT exam_name INTO v_exam_name FROM public.exams WHERE id = NEW.exam_id;\r\n\r\n  INSERT INTO public.notifications (title, message, target_type, is_read, branch_id, financial_year_id)\r\n  SELECT\r\n    'Exam Results Published',\r\n    'Results for \"' || v_exam_name || '\" are now available.',\r\n    'All',\r\n    false,\r\n    e.branch_id,\r\n    e.financial_year_id\r\n  FROM public.exams e WHERE e.id = NEW.exam_id;\r\n\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_fee_payment()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  student_name_text TEXT;\r\nBEGIN\r\n  SELECT s.first_name || ' ' || s.last_name\r\n  INTO student_name_text\r\n  FROM students s\r\n  JOIN student_fees sf ON sf.student_id = s.id\r\n  WHERE sf.id = NEW.student_fee_id;\r\n\r\n  INSERT INTO notifications (title, message, target_type, is_read, branch_id, financial_year_id)\r\n  VALUES (\r\n    'Fee Payment Received',\r\n    'Payment of ₹' || NEW.amount || ' received from ' || student_name_text || '.',\r\n    'All',\r\n    false,\r\n    NEW.branch_id,\r\n    NEW.financial_year_id\r\n  );\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_inquiry_converted()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  IF NEW.status = 'Joined' AND OLD.status != 'Joined' THEN\r\n    INSERT INTO notifications (title, message, target_type, is_read, branch_id, financial_year_id)\r\n    VALUES (\r\n      'Inquiry Converted',\r\n      'Inquiry \"' || NEW.inquiry_no || '\" for \"' || NEW.student_name || '\" converted to admission.',\r\n      'All',\r\n      false,\r\n      NEW.branch_id,\r\n      NEW.financial_year_id\r\n    );\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_leave_status_change()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_user_id uuid;\r\n  v_msg text;\r\nBEGIN\r\n  -- Only act when status changes to Approved or Rejected\r\n  IF (TG_OP = 'UPDATE' AND NEW.status <> OLD.status AND NEW.status IN ('Approved', 'Rejected')) THEN\r\n\r\n    -- Find the teacher's user_id\r\n    SELECT user_id INTO v_user_id FROM teachers WHERE id = NEW.teacher_id;\r\n\r\n    IF v_user_id IS NOT NULL THEN\r\n      v_msg := 'Your leave request (' || NEW.start_date || ' to ' || NEW.end_date || ') has been ' || NEW.status || '.';\r\n      IF NEW.admin_remarks IS NOT NULL AND NEW.admin_remarks <> '' THEN\r\n        v_msg := v_msg || ' Admin remark: ' || NEW.admin_remarks;\r\n      END IF;\r\n\r\n      INSERT INTO public.notifications (user_id, title, message, is_read)\r\n      VALUES (v_user_id, 'Leave ' || NEW.status, v_msg, false);\r\n    END IF;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_new_batch()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  INSERT INTO notifications (title, message, target_type, is_read, branch_id, financial_year_id)\r\n  VALUES (\r\n    'New Batch Created',\r\n    'Batch \"' || NEW.batch_name || '\" has been created.',\r\n    'All',\r\n    false,\r\n    NEW.branch_id,\r\n    NEW.financial_year_id\r\n  );\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_new_course()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  INSERT INTO notifications (title, message, target_type, is_read, branch_id, financial_year_id)\r\n  VALUES (\r\n    'New Course Added',\r\n    'Course \"' || NEW.course_name || '\" has been created.',\r\n    'All',\r\n    false,\r\n    NEW.branch_id,\r\n    NEW.financial_year_id\r\n  );\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_new_student()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nBEGIN\r\n  -- Send notification to all admin users (using profiles table)\r\n  PERFORM add_notification(\r\n    p.id,                               -- user_id (UUID)\r\n    'New Student Enrolled',\r\n    format('Student %s %s has been enrolled.', NEW.first_name, NEW.last_name),\r\n    '/dashboard/students/' || NEW.id\r\n  )\r\n  FROM profiles p\r\n  WHERE p.role IN ('admin', 'super_admin');\r\n  \r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_new_subject()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  course_name_text TEXT;\r\nBEGIN\r\n  SELECT course_name INTO course_name_text FROM courses WHERE id = NEW.course_id;\r\n  INSERT INTO notifications (title, message, target_type, is_read, branch_id, financial_year_id)\r\n  VALUES (\r\n    'New Subject Added',\r\n    'Subject \"' || NEW.subject_name || '\" added to course \"' || course_name_text || '\".',\r\n    'All',\r\n    false,\r\n    NEW.branch_id,\r\n    NEW.financial_year_id\r\n  );\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_new_teacher()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  INSERT INTO notifications (title, message, target_type, is_read, branch_id, financial_year_id)\r\n  VALUES (\r\n    'New Teacher Added',\r\n    'Teacher \"' || NEW.first_name || ' ' || NEW.last_name || '\" has joined.',\r\n    'All',\r\n    false,\r\n    NEW.branch_id,\r\n    NEW.financial_year_id\r\n  );\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_online_class_created()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  student_record RECORD;\r\nBEGIN\r\n  FOR student_record IN\r\n    SELECT s.user_id, s.first_name, s.last_name\r\n    FROM student_batches sb\r\n    JOIN students s ON s.id = sb.student_id\r\n    WHERE sb.batch_id = NEW.batch_id AND sb.status = 'active'\r\n  LOOP\r\n    IF student_record.user_id IS NOT NULL AND public.user_exists(student_record.user_id) THEN\r\n      PERFORM add_notification(\r\n        student_record.user_id,\r\n        'New Online Class Scheduled',\r\n        'Class \"' || NEW.title || '\" has been scheduled on ' || NEW.start_time || '.',\r\n        '/online-classes'\r\n      );\r\n    END IF;\r\n  END LOOP;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_online_class_live()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  student_record RECORD;\r\nBEGIN\r\n  IF NEW.status = 'live' AND OLD.status != 'live' THEN\r\n    FOR student_record IN\r\n      SELECT s.user_id, s.first_name, s.last_name\r\n      FROM student_batches sb\r\n      JOIN students s ON s.id = sb.student_id\r\n      WHERE sb.batch_id = NEW.batch_id AND sb.status = 'active'\r\n    LOOP\r\n      IF student_record.user_id IS NOT NULL AND public.user_exists(student_record.user_id) THEN\r\n        PERFORM add_notification(\r\n          student_record.user_id,\r\n          'Class is Live!',\r\n          'Class \"' || NEW.title || '\" is now live! Join now.',\r\n          '/online-classes/join/' || NEW.id\r\n        );\r\n      END IF;\r\n    END LOOP;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.notify_salary_payment()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_user_id uuid;\r\nBEGIN\r\n  SELECT user_id INTO v_user_id FROM teachers WHERE id = NEW.teacher_id;\r\n  IF v_user_id IS NOT NULL THEN\r\n    INSERT INTO public.notifications (user_id, title, message, is_read, branch_id, financial_year_id)\r\n    VALUES (\r\n      v_user_id,\r\n      'Salary Credited',\r\n      'A salary payment of ₹' || NEW.amount::text || ' has been recorded on ' || NEW.payment_date::text || '.',\r\n      false,\r\n      NEW.branch_id,\r\n      NEW.financial_year_id\r\n    );\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.record_input_tax()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  v_tax_rate_id  integer;\r\n  v_tax_rate     numeric;\r\n  v_input_cgst   integer;\r\n  v_input_sgst   integer;\r\n  v_journal_id   integer;\r\n  v_tax_amount   numeric;\r\n  v_tax_half     numeric;\r\nBEGIN\r\n  -- Only proceed if tax_amount is present and > 0\r\n  IF NEW.tax_amount IS NULL OR NEW.tax_amount <= 0 THEN\r\n    RETURN NEW;\r\n  END IF;\r\n\r\n  SELECT id INTO v_tax_rate_id FROM tax_rates WHERE is_default = true LIMIT 1;\r\n  SELECT rate INTO v_tax_rate FROM tax_rates WHERE id = v_tax_rate_id;\r\n  v_tax_amount := NEW.tax_amount;\r\n  v_tax_half := ROUND(v_tax_amount / 2, 2);\r\n\r\n  SELECT id INTO v_input_cgst FROM chart_of_accounts WHERE account_code = '2504';  -- Input CGST\r\n  SELECT id INTO v_input_sgst FROM chart_of_accounts WHERE account_code = '2505';  -- Input SGST\r\n\r\n  -- Create journal entry\r\n  INSERT INTO journal_entries (entry_date, reference, description, is_posted)\r\n  VALUES (COALESCE(NEW.expense_date, CURRENT_DATE), 'Expense #' || NEW.id, 'Input tax on expense', true)\r\n  RETURNING id INTO v_journal_id;\r\n\r\n  -- Debit Input CGST\r\n  INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n  VALUES (v_journal_id, v_input_cgst, v_tax_half, 0, 'Input CGST');\r\n\r\n  -- Debit Input SGST\r\n  INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n  VALUES (v_journal_id, v_input_sgst, v_tax_half, 0, 'Input SGST');\r\n\r\n  -- Credit Cash (reduce expense – the tax part was paid to vendor)\r\n  INSERT INTO journal_entry_lines (journal_entry_id, account_id, debit, credit, description)\r\n  VALUES (v_journal_id, (SELECT id FROM chart_of_accounts WHERE account_code = '1001'), 0, v_tax_amount, 'Cash paid for tax');\r\n\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.record_tax_on_income()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  -- Only insert if a tax_amount exists and is greater than zero\r\n  IF NEW.tax_amount IS NOT NULL AND NEW.tax_amount > 0 THEN\r\n    INSERT INTO public.tax_collections (\r\n      collection_date,\r\n      amount,\r\n      category,\r\n      income_id\r\n    ) VALUES (\r\n      NEW.income_date,\r\n      NEW.tax_amount,\r\n      'income',\r\n      NEW.id\r\n    );\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.record_tax_on_payment()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nBEGIN\r\n  IF NEW.tax_amount IS NOT NULL AND NEW.tax_amount > 0 THEN\r\n    INSERT INTO public.tax_collections (\r\n      student_fee_id,\r\n      payment_id,\r\n      collection_date,\r\n      amount,\r\n      category,\r\n      branch_id,\r\n      financial_year_id\r\n    ) VALUES (\r\n      NEW.student_fee_id,\r\n      NEW.id,\r\n      NEW.payment_date,\r\n      NEW.tax_amount,\r\n      'fee_payment',\r\n      NEW.branch_id,            -- ← inherited from fee_payment\r\n      NEW.financial_year_id\r\n    );\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.refresh_attendance_summary()\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\r\nBEGIN\r\n  -- Clear previous summary\r\n  DELETE FROM public.attendance_summary;\r\n\r\n  -- Compute fresh summary\r\n  INSERT INTO public.attendance_summary (student_id, batch_id, month_year, present, total, percentage, branch_id, financial_year_id)\r\n  SELECT\r\n    sa.student_id,\r\n    ses.batch_id,\r\n    to_char(ses.attendance_date, 'YYYY-MM'),\r\n    COUNT(*) FILTER (WHERE sa.status = 'Present'),\r\n    COUNT(*),\r\n    ROUND(100.0 * COUNT(*) FILTER (WHERE sa.status = 'Present') / NULLIF(COUNT(*), 0), 2),\r\n    ses.branch_id,\r\n    ses.financial_year_id\r\n  FROM public.student_attendance sa\r\n  JOIN public.attendance_sessions ses ON ses.id = sa.session_id\r\n  GROUP BY sa.student_id, ses.batch_id, to_char(ses.attendance_date, 'YYYY-MM'), ses.branch_id, ses.financial_year_id;\r\n\r\n  -- Insert notifications for students below 75%\r\n  INSERT INTO public.notifications (title, message, target_type, branch_id, financial_year_id)\r\n  SELECT\r\n    'Low Attendance Alert',\r\n    s.first_name || ' ' || s.last_name || ' has only ' || summary.percentage || '% attendance in ' || b.batch_name,\r\n    'All',\r\n    summary.branch_id,\r\n    summary.financial_year_id\r\n  FROM public.attendance_summary summary\r\n  JOIN public.students s ON s.id = summary.student_id\r\n  JOIN public.batches b ON b.id = summary.batch_id\r\n  WHERE summary.percentage < 75;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.set_inquiry_no()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  year_month TEXT;\r\n  next_num INTEGER;\r\nBEGIN\r\n  -- Only generate if not already provided\r\n  IF NEW.inquiry_no IS NULL THEN\r\n    year_month := to_char(CURRENT_DATE, 'YYYYMM');\r\n    -- Get next value, resetting per month is optional (can just use sequence)\r\n    -- For simplicity, use a single sequence across all months:\r\n    next_num := nextval('public.inquiry_no_seq');\r\n    NEW.inquiry_no := 'INQ-' || year_month || '-' || LPAD(next_num::TEXT, 4, '0');\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.set_invoice_number()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n    -- Generate number like INV-2026-0001\r\n    NEW.invoice_number := 'INV-' || to_char(NEW.invoice_date, 'YYYY') || '-' || LPAD(nextval('invoice_number_seq')::TEXT, 4, '0');\r\n    RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.split_gst(total_rate numeric, place_of_supply character varying, org_state character varying)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n  half_rate numeric;\r\nBEGIN\r\n  IF total_rate IS NULL OR total_rate = 0 THEN\r\n    RETURN '{\"cgst\":0, \"sgst\":0, \"igst\":0}'::jsonb;\r\n  END IF;\r\n  IF place_of_supply IS NULL OR org_state IS NULL OR place_of_supply = org_state THEN\r\n    half_rate := total_rate / 2;\r\n    RETURN jsonb_build_object('cgst', half_rate, 'sgst', half_rate, 'igst', 0);\r\n  ELSE\r\n    RETURN jsonb_build_object('cgst', 0, 'sgst', 0, 'igst', total_rate);\r\n  END IF;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.sync_medium_text()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  IF NEW.medium_id IS NOT NULL THEN\r\n    SELECT name INTO NEW.medium FROM mediums WHERE id = NEW.medium_id;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.test_dashboard()\n RETURNS jsonb\n LANGUAGE plpgsql\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_org_id integer;\r\n  v_branch_ids integer[];\r\nBEGIN\r\n  SELECT organization_id INTO v_org_id FROM public.profiles WHERE id = auth.uid();\r\n  v_branch_ids := ARRAY(SELECT id FROM public.branches WHERE organization_id = v_org_id);\r\n  RETURN jsonb_build_object('student_count', (\r\n    SELECT COUNT(*) FROM public.students WHERE branch_id = ANY(v_branch_ids) AND deleted_at IS NULL\r\n  ));\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.text_to_bytea(data text)\n RETURNS bytea\n LANGUAGE c\n IMMUTABLE STRICT\nAS '$libdir/http', $function$text_to_bytea$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.trg_audit_log()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'pg_catalog', 'public'\nAS $function$\r\nDECLARE\r\n  v_user_id uuid;\r\n  v_org_id integer;\r\n  v_branch_id bigint;\r\n  v_action text;\r\n  v_entity_id bigint;\r\n  v_changes jsonb;\r\nBEGIN\r\n  -- Get the current user (if any)\r\n  v_user_id := auth.uid();\r\n  IF v_user_id IS NULL THEN\r\n    -- No authenticated user (e.g. background job) → skip logging\r\n    RETURN COALESCE(NEW, OLD);\r\n  END IF;\r\n\r\n  -- Get user's org and branch\r\n  SELECT organization_id, branch_id INTO v_org_id, v_branch_id\r\n    FROM public.profiles WHERE id = v_user_id;\r\n\r\n  -- Determine action and the entity ID (the row's primary key)\r\n  IF TG_OP = 'INSERT' THEN\r\n    v_action := 'INSERT';\r\n    v_entity_id := NEW.id;\r\n    v_changes := to_jsonb(NEW);\r\n  ELSIF TG_OP = 'UPDATE' THEN\r\n    v_action := 'UPDATE';\r\n    v_entity_id := NEW.id;\r\n    -- Store changed columns only\r\n    v_changes := jsonb_build_object('old', to_jsonb(OLD), 'new', to_jsonb(NEW));\r\n  ELSIF TG_OP = 'DELETE' THEN\r\n    v_action := 'DELETE';\r\n    v_entity_id := OLD.id;\r\n    v_changes := to_jsonb(OLD);\r\n  END IF;\r\n\r\n  INSERT INTO public.activity_logs (\r\n    user_id, organization_id, branch_id,\r\n    action, entity_type, entity_id, changes,\r\n    ip_address, user_agent\r\n  ) VALUES (\r\n    v_user_id, v_org_id, v_branch_id,\r\n    v_action, TG_TABLE_NAME, v_entity_id, v_changes,\r\n    inet_client_addr(),\r\n    current_setting('request.headers', true)::json->>'user-agent'\r\n  );\r\n\r\n  RETURN COALESCE(NEW, OLD);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.update_attendance_summary()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n    v_student_id INTEGER;\r\n    v_session_id INTEGER;\r\n    v_batch_id INTEGER;\r\n    v_month_year TEXT;\r\n    v_present_count INTEGER;\r\n    v_total_count INTEGER;\r\nBEGIN\r\n    v_student_id := COALESCE(NEW.student_id, OLD.student_id);\r\n    v_session_id := COALESCE(NEW.session_id, OLD.session_id);\r\n    IF v_student_id IS NULL OR v_session_id IS NULL THEN RETURN NULL; END IF;\r\n\r\n    SELECT batch_id, attendance_date INTO v_batch_id, v_month_year \r\n    FROM attendance_sessions WHERE id = v_session_id AND deleted_at IS NULL;\r\n    IF v_batch_id IS NULL THEN RETURN NULL; END IF;\r\n\r\n    v_month_year := TO_CHAR(v_month_year, 'YYYY-MM');\r\n\r\n    SELECT \r\n        COUNT(*) FILTER (WHERE sa.check_in IS NOT NULL),\r\n        COUNT(*)\r\n    INTO v_present_count, v_total_count\r\n    FROM student_attendance sa\r\n    JOIN attendance_sessions a_s ON sa.session_id = a_s.id\r\n    WHERE sa.student_id = v_student_id\r\n      AND a_s.batch_id = v_batch_id\r\n      AND TO_CHAR(a_s.attendance_date, 'YYYY-MM') = v_month_year\r\n      AND a_s.deleted_at IS NULL;\r\n\r\n    INSERT INTO attendance_summary (\r\n        student_id,\r\n        batch_id,\r\n        month_year,\r\n        present,\r\n        total,\r\n        percentage\r\n    )\r\n    VALUES (\r\n        v_student_id,\r\n        v_batch_id,\r\n        v_month_year,\r\n        v_present_count,\r\n        v_total_count,\r\n        ROUND((v_present_count::numeric / NULLIF(v_total_count, 0) * 100), 2)\r\n    )\r\n    ON CONFLICT (student_id, batch_id, month_year) \r\n    DO UPDATE SET\r\n        present = EXCLUDED.present,\r\n        total = EXCLUDED.total,\r\n        percentage = EXCLUDED.percentage;\r\n\r\n    RETURN COALESCE(NEW, OLD);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.update_enrollment_attendance()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nDECLARE\r\n    v_student_id INTEGER;\r\n    v_session_id INTEGER;\r\n    v_batch_id INTEGER;\r\nBEGIN\r\n    v_student_id := COALESCE(NEW.student_id, OLD.student_id);\r\n    v_session_id := COALESCE(NEW.session_id, OLD.session_id);\r\n    IF v_student_id IS NULL OR v_session_id IS NULL THEN RETURN NULL; END IF;\r\n\r\n    SELECT batch_id INTO v_batch_id FROM attendance_sessions WHERE id = v_session_id AND deleted_at IS NULL;\r\n    IF v_batch_id IS NULL THEN RETURN NULL; END IF;\r\n\r\n    -- Calculate present count based on check_in, total sessions for this student in this batch\r\n    WITH stats AS (\r\n        SELECT \r\n            COUNT(*) AS total,\r\n            COUNT(*) FILTER (WHERE sa.check_in IS NOT NULL) AS present_count\r\n        FROM student_attendance sa\r\n        JOIN attendance_sessions a_s ON sa.session_id = a_s.id\r\n        WHERE sa.student_id = v_student_id\r\n          AND a_s.batch_id = v_batch_id\r\n          AND a_s.deleted_at IS NULL\r\n    )\r\n    UPDATE student_enrollments\r\n    SET attendance_percentage = ROUND((present_count::numeric / NULLIF(total, 0) * 100), 2)\r\n    FROM stats\r\n    WHERE student_id = v_student_id\r\n      AND batch_id = v_batch_id\r\n      AND status = 'active'\r\n      AND deleted_at IS NULL;\r\n\r\n    RETURN COALESCE(NEW, OLD);\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.update_inventory_stock()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n    IF NEW.transaction_type = 'purchase' THEN\r\n        UPDATE inventory_items SET current_stock = current_stock + NEW.quantity WHERE id = NEW.item_id;\r\n    ELSIF NEW.transaction_type = 'issue' THEN\r\n        UPDATE inventory_items SET current_stock = current_stock - NEW.quantity WHERE id = NEW.item_id;\r\n    ELSIF NEW.transaction_type = 'adjustment' THEN\r\n        UPDATE inventory_items SET current_stock = current_stock + NEW.quantity WHERE id = NEW.item_id;\r\n    END IF;\r\n    RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.update_item_stock()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  IF NEW.transaction_type = 'purchase' OR NEW.transaction_type = 'adjustment' THEN\r\n    UPDATE inventory_items SET current_stock = current_stock + NEW.quantity, updated_at = NOW() WHERE id = NEW.item_id;\r\n  ELSIF NEW.transaction_type = 'issue' THEN\r\n    UPDATE inventory_items SET current_stock = current_stock - NEW.quantity, updated_at = NOW() WHERE id = NEW.item_id;\r\n  END IF;\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.update_stock(item_id integer, qty integer)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  UPDATE inventory_items SET current_stock = current_stock - qty WHERE id = item_id;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.update_updated_at()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  NEW.updated_at = now();\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.update_updated_at_column()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nBEGIN\r\n  NEW.updated_at = now();\r\n  RETURN NEW;\r\nEND;\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.urlencode(string bytea)\n RETURNS text\n LANGUAGE c\n IMMUTABLE STRICT\nAS '$libdir/http', $function$urlencode$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.urlencode(string character varying)\n RETURNS text\n LANGUAGE c\n IMMUTABLE STRICT\nAS '$libdir/http', $function$urlencode$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.urlencode(data jsonb)\n RETURNS text\n LANGUAGE c\n IMMUTABLE STRICT\nAS '$libdir/http', $function$urlencode_jsonb$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.user_exists(p_user_id uuid)\n RETURNS boolean\n LANGUAGE sql\n SECURITY DEFINER\nAS $function$\r\n  SELECT EXISTS (SELECT 1 FROM auth.users WHERE id = p_user_id);\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.user_role()\n RETURNS text\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT role FROM profiles WHERE id = auth.uid();\r\n$function$\n"
  },
  {
    "function_definition": "CREATE OR REPLACE FUNCTION public.user_role_details()\n RETURNS TABLE(role text, org_id integer, branch_id bigint)\n LANGUAGE sql\n STABLE SECURITY DEFINER\nAS $function$\r\n  SELECT p.role, p.organization_id, p.branch_id\r\n  FROM public.profiles p\r\n  WHERE p.id = auth.uid()\r\n  LIMIT 1;\r\n$function$\n"
  }
]